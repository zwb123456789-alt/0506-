# OCS-图像联合仿真与姿态反演项目总控

最后更新：2026-06-30

## 1. 当前总状态

本项目已进入 `v0.4 BlenderOCS 重启工作区`。大根目录只作为全局索引与历史材料入口；当前有效工作、路线审阅和后续论文规划，均以：

```text
项目重启_v0.4_BlenderOCS/AGENTS.md
```

为权威入口。

当前阶段结论：

```text
阶段 I：旧仿真与反演实验已完成，作为历史资产保留。
阶段 II：v0.3 论文写作已封存，不直接恢复为投稿稿。
阶段 III：v0.4 BlenderOCS 重启进行中，四路线总裁决已完成；当前路线一 C 已推进到头B B6-FIX01，等待 Claude 多折补齐与 fold-matched baseline 修正结果后由 Codex 审阅。
```

## 2. 当前科学主线

v0.4 不再写成“真实未知目标姿态反演系统”，而是转为：

```text
model-known 条件下，独立 OCS 光度通道与图像成像通道共享同一物理前向模型时，
跨几何 OCS 多观测光度向量与图像通道对姿态信息的可观测性、互补性和置信一致性研究。
```

具体主线、路线状态、读取规则和红线，以 `项目重启_v0.4_BlenderOCS/AGENTS.md` 为准。

## 3. 新对话读取规则

如果新对话从大根目录开始，默认只读：

```text
AGENTS.md
项目重启_v0.4_BlenderOCS/AGENTS.md
```

读完后先简短确认：

```text
1. 当前项目焦点是否为 v0.4 BlenderOCS
2. 当前是否已进入路线一 C 头B B6-FIX01 等待 Claude 结果与 Codex 审阅阶段
3. 本轮任务是否需要进入 v0.4 子目录继续按其轻量读取规则读取
```

不要在大根目录一次性读取旧实验、旧论文、归档材料或完整历史档案。进入 v0.4 工作后，继续遵循 `项目重启_v0.4_BlenderOCS/AGENTS.md` 中的轻量上下文恢复规则。

## 4. 当前工作入口

当前唯一主工作区：

```text
项目重启_v0.4_BlenderOCS/
```

其内部当前结构：

```text
01_v0.3封存/
02_重大分支路线图/
03_项目说明与规划材料/
04_四路线分工区/
05_归档区/
AGENTS.md
```

当前 v0.4 最新状态：

```text
C3 已不是“待放行”状态，而是已经完成正式 5-fold 并由 R76 审阅稳定为负结果。
C2/C3 三通道负结果证据链已由 R77 稳定。
C2/C3 Results 非正文总材料包已由 R78 稳定并进入成果区。
E45A/E45B 已稳定：exact-bin yaw=0.00% 不能读作 yaw 信息完全不存在，论文主叙事应写成 protocol-defined yaw extrapolation gap，exact-bin 0% 只作为哨兵指标。
头A 已由 R90 收口；头B 已完成 B1/B2/B3/P0/B4/P1-A/B5 阶段性材料。R104 裁定 100 不是头B闭口，而是进入 B6 A类同门真改进；Claude 101 已由 R108 判定为 B6 fold0 pilot/code 初版，部分接收但不闭口；R109 已下达 B6-FIX01 多折补齐与 fold-matched baseline 修正任务。
R110/R111 已保留作者关于现有结果、4维 OCS 现实性、yaw-block 外推、1C 目标、小项目目标和光变/时序方向的关键问答与路线一复位记忆。
```

当前下一步以 `项目重启_v0.4_BlenderOCS/AGENTS.md` 与 `项目重启_v0.4_BlenderOCS/CLAUDE.md` 为准；当前等待 Claude 按 R109 返回 B6-FIX01/102 或后续执行报告，再由 Codex 审阅。B6-FIX01 只能判断判据/训练头/augmentation 等问题，不能闭整个路线一 C，不能触发头A/头B大合并裁决，不直接启动 T3/T4、论文正文正式改写、三轴小项目或路线二/三/四扩展。

## 5. 历史材料定位

旧阶段材料只按需查阅，不作为当前主线依据。

```text
进度档案_仿真与反演_full.md
论文改进/
ocs_project/
结果/
项目重启_v0.4_BlenderOCS/01_v0.3封存/
```

用途说明：

- `进度档案_仿真与反演_full.md`：阶段 I 完整历史。
- `论文改进/`：v0.3 写作与补充实验旧材料。
- `ocs_project/`、`结果/`：旧代码与实验产物。
- `项目重启_v0.4_BlenderOCS/01_v0.3封存/`：当前工作区内的旧稿封存入口。

## 6. 根目录红线

- 大根目录 `AGENTS.md` 只做总控索引，不承载 v0.4 细节正文。
- 当前项目判断以 `项目重启_v0.4_BlenderOCS/AGENTS.md` 为准。
- 不把 v0.3 旧稿直接恢复为投稿稿。
- 不把旧模块 A face-center OCS 与 v0.4 Blender-derived OCS 混写成同一结果链。
- 不声称真实望远镜验证、field-proven 或 operational-ready。
- 不默认读取历史全量材料；必须按任务需要逐步读取。
- 当前以 R104/R108/R109/R110/R111 为准；未另行放行前，不触发头A/头B大合并裁决，不把 B6 当作路线一 C 整体闭口，不进入 T3/T4 正式阶段，不写论文正文，不进成果区，不启动三轴小项目或路线二/三/四扩展。

## 7. 给新对话的一句话

先读本文件确认项目大局，然后进入 `项目重启_v0.4_BlenderOCS/AGENTS.md`；后续一切以 v0.4 子目录的轻量读取规则和当前红线为准。
