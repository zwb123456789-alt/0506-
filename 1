Update Todos

精简 CLAUDE.md 历史记录

说明 U 型块同部件遮挡原因

申请下一步脚本增强改动
已按要求精简 CLAUDE.md 的 §八：删除流水账，只保留对后续流程有用的基线、路线、遮挡验证结论。

关于 U 型块问题：

你说“面元小，所以排除自身影响不大”这个判断只对同一个面元的自相交成立，但不适用于“同一部件内部不同面元之间的遮挡”。

当前逻辑是：


exclude_parts={mat_name}
它排除的是整个部件，不是“当前这个面元”。

所以即使 U 型块有很多小面元，只要它们都属于同一个 STL / 同一个 mat_name，那么：

当前采样面元会被排除；
U 型背墙的其他面元也会被一起排除；
因此同部件内部遮挡会漏检。
正确目标应是：


不排除当前部件整体；
只排除当前射线起点所在的那个三角面，或过滤极近 hit distance。
也就是说，面元级方法本来可以解决问题，但当前代码还没做到“只排除起始面元”，而是粗粒度地排除了整个部件。

下一步我建议继续做两件事：

给真实模型抽查补三维局部标注图。
修复图中文字字体缺字问题。
需要改动：

[改动申请] ocs_project/05_occlusion_validation/run_occlusion_validation.py｜增强遮挡验证输出｜增加真实模型抽查点三维标注图；设置中文字体；保持一条命令复现｜风险低，仅影响独立验证模块

[改动申请] CLAUDE.md｜记录本轮人工检查确认与下一步｜保持进度档案精简｜风险低，仅更新文档

请确认是否继续。