# OCS-图像联合仿真与姿态反演项目总控

最后更新：2026-06-29

## 1. 当前总状态

本项目已进入 `v0.4 BlenderOCS 重启工作区`。大根目录只作为全局索引与历史材料入口；当前有效工作、路线审阅和后续论文规划，均以：

```text
项目重启_v0.4_BlenderOCS/CLAUDE.md
```

为权威入口。

当前阶段结论：

```text
阶段 I：旧仿真与反演实验已完成，作为历史资产保留。
阶段 II：v0.3 论文写作已封存，不直接恢复为投稿稿。
阶段 III：v0.4 BlenderOCS 重启进行中，四路线总裁决已完成；当前路线一 C 已推进到头B-B3/P0 只读诊断初版，最新细节以 `项目重启_v0.4_BlenderOCS/CLAUDE.md` 为准。
```

## 2. 当前科学主线

v0.4 不再写成“真实未知目标姿态反演系统”，而是转为：

```text
model-known 条件下，独立 OCS 光度通道与图像成像通道共享同一物理前向模型时，
跨几何 OCS 多观测光度向量与图像通道对姿态信息的可观测性、互补性和置信一致性研究。
```

具体主线、路线状态、读取规则和红线，以 `项目重启_v0.4_BlenderOCS/CLAUDE.md` 为准。

## 3. 新对话读取规则

如果新对话从大根目录开始，默认只读：

```text
CLAUDE.md
项目重启_v0.4_BlenderOCS/CLAUDE.md
```

读完后先简短确认：

```text
1. 当前项目焦点是否为 v0.4 BlenderOCS
2. 当前是否已进入路线一 C 的头B-B3/P0 只读诊断补齐点
3. 本轮任务是否需要进入 v0.4 子目录继续按其轻量读取规则读取
```

不要在大根目录一次性读取旧实验、旧论文、归档材料或完整历史档案。进入 v0.4 工作后，继续遵循 `项目重启_v0.4_BlenderOCS/CLAUDE.md` 中的轻量上下文恢复规则。

## 4. 当前工作入口

当前唯一主工作区：

```text
项目重启_v0.4_BlenderOCS/
```

其内部当前结构：

```text
01_v0.3封存/
02_重大分支路线图/
03_项目说明与规划材料/
04_四路线分工区/
05_归档区/
CLAUDE.md
```

当前 v0.4 最新状态：

```text
C3 已不是“待放行”状态，而是已经完成正式 5-fold 并由 R76 审阅稳定为负结果。
C2/C3 负结果证据链、Results 非正文材料、负结果归因、指标重构、图表/SI 与头A桥接材料已分别由 R77/R78/R80/R82/R83/R86/R88/R90 稳定。
头B-B1 文献检索由 R91 完成；头B-B2 的 93 方法总结已接收；R95 已下发 B-3/P0 只读诊断任务单；Claude 94 已提交 P0 只读诊断初版；R96 判定 94 合规但 P0 尚未闭口。
```

当前下一步以 `项目重启_v0.4_BlenderOCS/CLAUDE.md` 为准；原则上先补齐 P0-2/P0-3/P0-4 的只读矩阵、图和表，并直接读取 V0.3 原始 metrics/split 文件完成协议对齐证据；不直接启动 P1/P2、新训练、论文正文正式改写、三轴小项目或路线二/三/四扩展。

## 5. 历史材料定位

旧阶段材料只按需查阅，不作为当前主线依据。

```text
进度档案_仿真与反演_full.md
论文改进/
ocs_project/
结果/
项目重启_v0.4_BlenderOCS/01_v0.3封存/
```

用途说明：

- `进度档案_仿真与反演_full.md`：阶段 I 完整历史。
- `论文改进/`：v0.3 写作与补充实验旧材料。
- `ocs_project/`、`结果/`：旧代码与实验产物。
- `项目重启_v0.4_BlenderOCS/01_v0.3封存/`：当前工作区内的旧稿封存入口。

## 6. 根目录红线

- 大根目录 `CLAUDE.md` 只做总控索引，不承载 v0.4 细节正文。
- 当前项目判断以 `项目重启_v0.4_BlenderOCS/CLAUDE.md` 为准。
- 不把 v0.3 旧稿直接恢复为投稿稿。
- 不把旧模块 A face-center OCS 与 v0.4 Blender-derived OCS 混写成同一结果链。
- 不声称真实望远镜验证、field-proven 或 operational-ready。
- 不默认读取历史全量材料；必须按任务需要逐步读取。
- R96 后，未另行放行前，不进入 P1/P2、头A/头B大合并裁决、成果区归档、档 B 新训练、后验架构/超参补救、论文正文正式改写、三轴小项目或路线二/三/四扩展。

## 7. 给新对话的一句话

先读本文件确认项目大局，然后进入 `项目重启_v0.4_BlenderOCS/CLAUDE.md`；后续一切以 v0.4 子目录的轻量读取规则和当前红线为准。
