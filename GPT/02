## 🤖 Assistant

可以。你下一步不要急着继续扩展轨道，也不要先做复杂相机模型。建议你按下面这个顺序推进。

---

# 一、下一步总体操作顺序

你现在要做的是建立一个可靠的：

> **STL模型 → 材料BRDF → 姿态变化 → 遮挡修正 → OCS曲线 → 姿态光度图像**

的闭环流程。

建议分成 6 个阶段。

---

# 阶段 1：先整理模型和材料

## 1. 确认 STL 单位

你现在代码默认：

```text
STL单位 = mm
```

因为最后乘了：

```text
1e-6
```

所以你要先确认 Creo 导出的 STL 单位到底是：

- mm；
- cm；
- m。

如果 STL 是 mm，继续使用 `1e-6` 没问题。  
如果 STL 是 m，就不能再乘 `1e-6`。

这是第一件必须确认的事，因为它直接决定 OCS 数值量级。

---

## 2. 确认模型坐标系

你需要明确卫星本体系：

```text
+X 指向哪里？
+Y 指向哪里？
+Z 指向哪里？
```

例如：

```text
+X：卫星前向
+Y：太阳翼展开方向
+Z：天顶方向
```

后续所有姿态角、太阳方向、观测方向都要基于这个坐标系。  
否则 OCS 曲线和图像虽然能算出来，但很难解释。

---

## 3. 确认不同 STL 部件和材料对应关系

你现在有：

```text
dulvmo.stl → dulvmo 材料
qita.stl   → qita 材料
```

下一步建议你整理成表格：

| 部件 STL | 物理部件 | 材料名 | 漫反射系数 | 镜面系数 | 高光指数 |
|---|---|---|---|---|---|
| dulvmo.stl | 镀铝膜/反光层 | dulvmo | 0.20 | 0.60 | 80 |
| qita.stl | 其他主体 | qita | 0.40 | 0.15 | 25 |

先不追求材料参数完全真实，但必须做到：

```text
每个部件属于哪个材料是明确的。
```

---

# 阶段 2：先做一个固定几何条件下的单点验证

不要一上来就扫很多角度。  
先固定一个最简单场景。

例如：

```text
太阳方向：+X 方向
观测方向：+X 方向
卫星姿态：不旋转
```

也就是：

```text
太阳、观测器都在卫星正前方。
```

然后计算一次：

```text
OCS_single
```

同时输出：

```text
被太阳照亮的面数量
被观测器看到的面数量
同时满足照亮和可见的面数量
经过遮挡修正后的面数量
最终参与积分的面数量
```

你不要只看最终 OCS，要先看这些中间统计量。

你应该得到类似：

```text
总面元数：100000
朝向太阳面元：42000
朝向观测器面元：42000
同时满足几何条件面元：42000
太阳方向被遮挡面元：3000
观测方向被遮挡面元：2500
最终有效面元：36500
```

如果这些数量完全异常，比如：

```text
有效面元 = 0
```

或者：

```text
几乎所有面元都被遮挡
```

那说明方向定义、法向、射线方向或 epsilon 有问题。

---

# 阶段 3：重点验证遮挡修正是否正确

你特别提到：

> 需要在判断遮挡修正时进行一个验证。

这一步非常关键。建议你做 5 类验证。

---

# 二、遮挡修正验证方案

## 验证 1：关闭遮挡 vs 开启遮挡对比

你需要做两组计算：

### A. 不做二次遮挡

只判断：

```text
面元法向 · 太阳方向 > 0
面元法向 · 观测方向 > 0
```

得到：

```text
OCS_no_occlusion
```

### B. 开启遮挡

同时判断：

```text
太阳方向不被遮挡
观测方向不被遮挡
```

得到：

```text
OCS_with_occlusion
```

理论上应该满足：

```text
OCS_with_occlusion ≤ OCS_no_occlusion
```

如果出现：

```text
OCS_with_occlusion > OCS_no_occlusion
```

说明你的遮挡逻辑一定有问题。

建议每个姿态都保存：

```text
遮挡前有效面元数
遮挡后有效面元数
遮挡前 OCS
遮挡后 OCS
遮挡修正比例
```

例如：

```text
occlusion_ratio = OCS_with_occlusion / OCS_no_occlusion
```

正常情况下它应该在：

```text
0 ~ 1
```

之间。

---

## 验证 2：用简单几何体做基准测试

不要一开始只用复杂卫星模型验证遮挡。  
你应该专门做几个简单模型。

### 测试模型 A：单个平板

例如一个正方形薄板。

理论现象：

- 从正面看，无遮挡；
- 从背面看，不可见；
- 遮挡修正不应该改变结果。

也就是说：

```text
OCS_no_occlusion ≈ OCS_with_occlusion
```

如果单平板都出现大量遮挡，说明你的射线起点偏移或方向有问题。

---

### 测试模型 B：两个平行平板前后排列

例如：

```text
前板在 x = 0
后板在 x = -1
太阳/观测器从 +X 方向看
```

理论现象：

- 前板可见；
- 后板被前板遮挡；
- 开启遮挡后，后板贡献应消失或明显减少。

这可以验证：

```text
观测方向遮挡是否有效。
```

---

### 测试模型 C：一个板遮住另一个板的太阳光

例如：

```text
太阳从 +X 方向照射
观测器从 +Y 方向观察
```

某些面虽然能被观测器看到，但不被太阳照亮，因为被其他结构挡住。

这可以验证：

```text
太阳方向阴影遮挡是否有效。
```

---

### 测试模型 D：凹形结构 / U形结构

卫星经常有凹陷结构、舱体遮挡、太阳翼遮挡。

U 形模型可以验证：

```text
自遮挡是否合理
内部面是否被错误保留
```

---

## 验证 3：可视化被遮挡面元

这是最重要的验证手段。

你需要输出一张 3D 可视化图，把面元分成几类颜色：

| 面元状态 | 颜色 |
|---|---|
| 不朝向太阳或不朝向观测器 | 灰色 |
| 朝向太阳和观测器，且无遮挡 | 绿色 |
| 被太阳遮挡 | 蓝色 |
| 被观测方向遮挡 | 红色 |
| 同时被太阳和观测遮挡 | 紫色 |

这样你能直观看出：

- 是不是背面被错误认为可见；
- 太阳翼后方阴影是否正确；
- 主体是否遮住其他部件；
- 遮挡边界是否合理；
- 有没有大量面元被错误标红。

这一步比只看 OCS 数值更可靠。

你至少要对下面几种姿态做可视化：

```text
姿态 1：yaw = 0°
姿态 2：yaw = 45°
姿态 3：yaw = 90°
姿态 4：pitch = 45°
姿态 5：roll = 45°
```

---

## 验证 4：检查 epsilon 敏感性

你的代码里有：

```text
epsilon = 0.01
```

这个值用于把射线起点从面元中心沿法向稍微偏移，避免射线击中自身面元。

但 epsilon 太小或太大都会出问题。

### 如果 epsilon 太小

可能出现：

```text
面元被自己遮挡
```

导致大量面元误判为遮挡。

### 如果 epsilon 太大

可能出现：

```text
射线起点已经穿过薄结构
```

导致该遮挡没检测到，或者检测结果跳变。

你需要做 epsilon 扫描，例如：

```text
epsilon = 1e-5 × 模型尺度
epsilon = 1e-4 × 模型尺度
epsilon = 1e-3 × 模型尺度
epsilon = 1e-2 × 模型尺度
```

不要固定写死 `0.01`，而是根据模型尺度设定。

例如模型最大尺寸为：

```text
L = 3000 mm
```

那么可以测试：

```text
epsilon = 0.003 mm
epsilon = 0.03 mm
epsilon = 0.3 mm
epsilon = 3 mm
```

你观察：

```text
有效面元数
OCS
遮挡比例
```

是否稳定。

理想情况是：

```text
epsilon 在一段合理范围内，OCS 变化很小。
```

如果 OCS 随 epsilon 大幅变化，说明遮挡判断不稳定。

---

## 验证 5：人工抽查射线

对复杂卫星模型，建议你随机抽取几个面元，人工检查：

```text
面元中心
面元法向
太阳方向
观测方向
是否被太阳遮挡
是否被观测遮挡
击中的遮挡面属于哪个部件
击中距离是多少
```

尤其要检查：

1. 明显暴露的外表面是否被误判遮挡；
2. 明显在背后的面是否被正确遮挡；
3. 太阳翼后方区域是否产生阴影；
4. 主体遮挡太阳翼时是否正确；
5. 射线是否击中自己所属的同一个三角面。

这一步可以帮你发现很多隐藏问题。

---

# 三、遮挡验证时你要特别注意的方向定义

这是最容易错的地方。

你代码里面使用：

```text
sun_direction
detector_direction
```

并且做了：

```text
dot(normal, sun_direction) > 0
dot(normal, detector_direction) > 0
```

这说明你当前默认：

```text
sun_direction = 从卫星指向太阳的方向
detector_direction = 从卫星指向探测器的方向
```

这个定义是可以的。

那么遮挡射线也应该从面元中心出发，沿着：

```text
sun_direction
detector_direction
```

发射。

如果你把方向理解成：

```text
太阳光传播方向 = 从太阳指向卫星
```

那就反了。

所以你必须统一：

```text
sun_direction：面元指向太阳
obs_direction：面元指向观测器
```

此时判断条件就是：

```text
normal · sun_direction > 0
normal · obs_direction > 0
```

这是合理的。

---

# 四、遮挡修正验证通过后，再做姿态扫描

遮挡验证完成之后，再进行姿态扫描。

建议你先固定：

```text
太阳方向固定
观测方向固定
```

然后只改变卫星姿态。

例如：

```text
yaw: 0° ~ 360°
pitch: 0°
roll: 0°
```

每 5° 或 10° 一个点。

每个姿态保存：

```text
yaw
pitch
roll
OCS_no_occlusion
OCS_with_occlusion
occlusion_ratio
有效面元数
图像
```

你最后画三条曲线：

```text
1. OCS_no_occlusion vs yaw
2. OCS_with_occlusion vs yaw
3. occlusion_ratio vs yaw
```

这样你能看出遮挡修正对不同姿态的影响。

如果某些角度出现突然尖峰或异常跳变，就回到那一帧做面元可视化。

---

# 五、然后再生成姿态光度图像

遮挡修正验证通过后，再开始图像部分。

## 图像生成顺序建议

### 第一步：先做面元中心散点图

每个有效面元投影成一个点。

点的亮度由：

```text
BRDF × cos_i
```

决定。

这一步不追求图像连续，只用来验证：

- 姿态变化是否正确；
- 亮区是否在合理位置；
- 高光是否随姿态移动；
- 被遮挡区域是否消失。

---

### 第二步：再做三角面投影图

把三角面完整填充到图像平面，使用 Z-buffer。

此时图像会更像真实二维目标。

---

### 第三步：做统一灰度归一化

姿态识别用图像时，归一化很关键。

建议你保存两种图：

#### A. 单帧归一化图

每张图自己归一化到 0~255。

优点：

```text
轮廓和亮暗结构清楚。
```

缺点：

```text
帧间总亮度信息丢失。
```

#### B. 全数据集统一归一化图

用全数据集最大亮度统一归一化。

优点：

```text
保留不同姿态之间的亮度差异。
```

缺点：

```text
暗姿态图像可能很暗。
```

科研建议：

```text
两种都保存。
```

---

# 六、你现在最具体的下一步任务清单

你可以直接按这个清单做。

---

## 第 1 天：模型检查

完成：

```text
1. 确认 STL 单位
2. 确认卫星本体坐标系
3. 检查 STL 法向是否正常
4. 检查每个部件材料对应关系
5. 记录模型最大尺寸 L
```

输出：

```text
model_check_report.txt
```

里面写：

```text
STL单位：
模型尺寸：
坐标系定义：
部件数量：
材料对应：
总面元数：
是否存在破面/反法向：
```

---

## 第 2 天：单姿态 OCS 验证

固定：

```text
sun_dir = +X
obs_dir = +X
attitude = identity
```

输出：

```text
OCS_no_occlusion
OCS_with_occlusion
有效面元数量统计
遮挡比例
```

然后换几个方向：

```text
sun_dir = +X, obs_dir = +Y
sun_dir = +X, obs_dir = -X
sun_dir = +Z, obs_dir = +X
```

检查是否符合直觉。

---

## 第 3 天：遮挡验证

完成 5 件事：

```text
1. 简单平板模型验证
2. 双平板遮挡验证
3. 凹形结构验证
4. epsilon 敏感性测试
5. 遮挡面元颜色可视化
```

这一阶段非常重要。  
如果这一步没通过，不建议继续做图像和姿态数据集。

---

## 第 4 天：姿态扫描

固定太阳和观测方向：

```text
sun_dir = normalize([1, 0, 0.3])
obs_dir = normalize([0, -1, 0.1])
```

扫描：

```text
yaw = 0° ~ 360°
pitch = 0°
roll = 0°
```

输出：

```text
OCS_no_occlusion vs yaw
OCS_with_occlusion vs yaw
occlusion_ratio vs yaw
```

检查曲线是否：

- 连续；
- 无异常跳变；
- 峰值位置能解释；
- 遮挡修正比例在合理范围内。

---

## 第 5 天：初版光度图像

先做：

```text
面元中心投影散点图
```

每 30° 输出一张：

```text
yaw_000.png
yaw_030.png
yaw_060.png
...
yaw_330.png
```

人工检查图像与姿态是否对应。

确认后再做三角面填充图。

---

# 七、最终推荐的验证标准

你的遮挡修正可以认为“基本通过”，当满足下面条件：

```text
1. OCS_with_occlusion 永远不大于 OCS_no_occlusion
2. 单平板模型中，遮挡修正几乎不改变结果
3. 双平板模型中，后方平板能被正确遮挡
4. 凹形结构内部遮挡关系合理
5. epsilon 在合理范围内变化时，OCS 不发生大幅跳变
6. 遮挡面元可视化结果符合直觉
7. 姿态扫描曲线没有无法解释的尖峰或断崖
```

如果这 7 条都满足，你再继续做大规模姿态图像数据集。

---

# 八、最重要的一句话

你下一步的重点不是“把代码写得更复杂”，而是：

> **先证明遮挡修正是可信的，再用它生成 OCS 曲线和姿态光度图像。**

建议你马上从这三个动作开始：

```text
1. 固定一个简单几何条件，输出遮挡前/后的面元数量和 OCS。
2. 做单平板、双平板、凹形结构三个遮挡验证模型。
3. 做遮挡面元三维颜色可视化。
```

这三步完成后，你的仿真结果才有继续用于科研姿态分析的基础。
