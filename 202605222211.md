收到，**Step 11e-B1 late fusion 验收通过，当前应暂停**。

结论可以这样写入 `CLAUDE.md/result.md`：

```text
Prediction-level sin/cos late fusion 已完成。
强 OCS 分支下，最佳 β≈0.93–0.96，CNN 权重极小，提升仅 1%左右；
弱 OCS total_log 下，最佳 β=0.11，CNN 主导，mean 从 31.95° 降到 11.99°，但本质接近 CNN-only，未体现强互补。
结论：prediction-level late fusion 互补性有限，下一步应转向 feature-level 双流联合训练。
```

建议下一步是：

```text
Step 11e-B2：feature-level CNN+OCS 双流联合训练
```

但在正式进入前，先做一个小检查：late fusion 里的 `all_raw OCS-only = 5.09°`，而之前 `train_mlp.py` 汇总表是 `3.98±0.6°`。这不一定是错误，可能是用了不同预测文件 / seed 聚合口径 / best seed vs mean seed，但需要在 `result.md` 里标注：

```text
late fusion 表中的 OCS-only 为本次对齐 prediction CSV 的 endpoint；
train_mlp.py 表中的 OCS-only 为原始 MLP 5 seeds 汇总。
```

如果要继续，我建议给 Claude 的下一步就是下面这段。

---

# Step 11e-B2 下一步：feature-level CNN+OCS 双流联合训练

## 当前状态

已完成：

```text
CNN image-only 10°→5°:
mean = 12.38±0.74°
Hit5 = 26.1%

OCS MLP all_raw:
mean = 3.98±0.6°
Hit5 = 90.7%

Prediction-level late fusion:
all_raw:      β=0.96, mean=5.03°，仅比 endpoint OCS-only 5.09° 提升 1.2%
per_part_log: β=0.93, mean=6.15°，仅比 endpoint OCS-only 6.24° 提升 1.4%
total_log:    β=0.11, mean=11.99°，主要由 CNN 主导
```

结论：

```text
sin/cos prediction late fusion 提升有限；
需要做 feature-level 双流联合训练，允许网络在中间表征层学习互补。
```

---

## 本轮任务

新增：

```text
ocs_project/03_inversion/train_fusion.py
```

目标：

```text
CNN image branch + OCS MLP branch -> feature concat -> regression head
```

暂时只做 feature-level fusion，不做更大 CNN、不做 224 分辨率、不做数据增强。

---

## 实验优先级

本轮跑 3 个 OCS case：

| case | OCS 特征 | 目的 |
|---|---|---|
| `all_raw` | concat5 all raw 45D | 测试能否超过最强 OCS MLP |
| `per_part_log` | concat5 per_part log 30D | 测试稳定 semi-oracle + image |
| `total_log` | concat5 total log 15D | 测试弱 OCS + image 是否互补 |

优先级：

```text
先 all_raw smoke
再 total_log smoke
确认代码正常后跑 3 cases × 5 seeds
```

---

## 模型结构建议

### Image branch

复用 `train_cnn.py` 里的 TinyCNN backbone，但去掉最后 regression head：

```text
1×128×128 image
Conv/GN/SiLU/Pool × 4
AdaptiveAvgPool2d(1)
Linear 128 -> 64
输出 img_feat: 64D
```

### OCS branch

```text
OCS input dim = 45 / 30 / 15
Linear dim -> 128
LayerNorm
SiLU
Dropout 0.10
Linear 128 -> 64
LayerNorm
SiLU
输出 ocs_feat: 64D
```

### Fusion head

```text
concat [img_feat, ocs_feat] = 128D
Linear 128 -> 128
LayerNorm
SiLU
Dropout 0.10
Linear 128 -> 64
SiLU
Linear 64 -> 4
```

输出仍然是：

```text
[sin(yaw), cos(yaw), sin(pitch), cos(pitch)]
```

预测时归一化 sin/cos 后 decode。

---

## 关键实现要求

### 1. split 必须一致

沿用 `train_mlp.py` / `train_cnn.py` 的：

```text
10° train pool -> 5° interpolation test
train pool 内 80/20 val
```

测试集应为：

```text
N_test = 1998
```

### 2. 图像对齐

图像路径：

```text
out_prefix -> *_brdf.png
```

必须确认：

```text
matched images = 2701
```

### 3. OCS 特征

OCS 根目录：

```text
结果/模块A_重构/multi_geom_ggx_yaw73_pitch37/run_20260520_162831
```

使用 concat5 多几何。

特征模式：

```text
all_raw:
  feat = all
  transform = raw

per_part_log:
  feat = per_part
  transform = log

total_log:
  feat = total
  transform = log
```

注意：

```text
zscore scaler 只能 fit train split，不能 fit 全体 2701。
```

### 4. 图像 intensity

先固定：

```text
--intensity log1p
--image-size 128
```

不要再跑 raw，之前已证明 log1p 更好。

### 5. DataLoader

Windows 默认：

```text
--num-workers 0
```

### 6. 训练参数

建议：

```text
epochs = 500
patience = 100
batch_size = 32
lr = 1e-3
weight_decay = 1e-4
seeds = 0 1 2 3 4
```

如果 all_raw 明显过拟合，可以追加一个小消融：

```text
--dropout 0.20
```

但第一轮先固定 0.10。

---

## 命令

### 0. 环境

用 Windows CMD，不要用 Git Bash。

```bat
chcp 65001 >nul

cd /d "D:\我的文件\研究生学术\光学项目\0506新\ocs_project\03_inversion"
conda activate ocs_sim

set PYTHONFAULTHANDLER=1
set PYTHONUNBUFFERED=1
set OMP_NUM_THREADS=1
set MKL_NUM_THREADS=1
set OPENBLAS_NUM_THREADS=1

set ROOT=D:\我的文件\研究生学术\光学项目\0506新
set OCS_ROOT=%ROOT%\结果\模块A_重构\multi_geom_ggx_yaw73_pitch37\run_20260520_162831
set SCAN_JSON=%OCS_ROOT%\phase63_backscatter\ocs_scan.json
set IMAGE_DIR=%ROOT%\结果\模块B_渲染\run_20260521_phase63_ggx
set OUT_ROOT=%ROOT%\结果\模块C_反演\cnn_ocs_fusion
```

---

### 1. smoke test：all_raw

```bat
python -u train_fusion.py ^
  --ocs-root "%OCS_ROOT%" ^
  --scan-json "%SCAN_JSON%" ^
  --image-dir "%IMAGE_DIR%" ^
  --out-root "%OUT_ROOT%" ^
  --geom-set concat5 ^
  --ocs-feat all ^
  --ocs-transform raw ^
  --image-size 128 ^
  --intensity log1p ^
  --split 10to5 ^
  --batch-size 32 ^
  --epochs 3 ^
  --patience 2 ^
  --lr 1e-3 ^
  --weight-decay 1e-4 ^
  --dropout 0.10 ^
  --seeds 0 ^
  --num-workers 0 ^
  --device auto ^
  --tag smoke_all_raw
```

必须确认：

```text
N_total = 2701
N_train_pool = 703 or 563? 需与 train_mlp/train_cnn 口径一致
N_test = 1998
matched images = 2701
OCS dim = 45
forward/backward 正常
metrics 无 NaN/Inf
```

如果 `train_mlp.py` 实际 train pool 是 563，则保持一致，不要临时改 split。

---

### 2. smoke test：total_log

```bat
python -u train_fusion.py ^
  --ocs-root "%OCS_ROOT%" ^
  --scan-json "%SCAN_JSON%" ^
  --image-dir "%IMAGE_DIR%" ^
  --out-root "%OUT_ROOT%" ^
  --geom-set concat5 ^
  --ocs-feat total ^
  --ocs-transform log ^
  --image-size 128 ^
  --intensity log1p ^
  --split 10to5 ^
  --batch-size 32 ^
  --epochs 3 ^
  --patience 2 ^
  --lr 1e-3 ^
  --weight-decay 1e-4 ^
  --dropout 0.10 ^
  --seeds 0 ^
  --num-workers 0 ^
  --device auto ^
  --tag smoke_total_log
```

确认：

```text
OCS dim = 15
```

---

## 正式实验

### 3. all_raw × 5 seeds

```bat
python -u train_fusion.py ^
  --ocs-root "%OCS_ROOT%" ^
  --scan-json "%SCAN_JSON%" ^
  --image-dir "%IMAGE_DIR%" ^
  --out-root "%OUT_ROOT%" ^
  --geom-set concat5 ^
  --ocs-feat all ^
  --ocs-transform raw ^
  --image-size 128 ^
  --intensity log1p ^
  --split 10to5 ^
  --batch-size 32 ^
  --epochs 500 ^
  --patience 100 ^
  --lr 1e-3 ^
  --weight-decay 1e-4 ^
  --dropout 0.10 ^
  --seeds 0 1 2 3 4 ^
  --num-workers 0 ^
  --device auto ^
  --tag all_raw
```

---

### 4. per_part_log × 5 seeds

```bat
python -u train_fusion.py ^
  --ocs-root "%OCS_ROOT%" ^
  --scan-json "%SCAN_JSON%" ^
  --image-dir "%IMAGE_DIR%" ^
  --out-root "%OUT_ROOT%" ^
  --geom-set concat5 ^
  --ocs-feat per_part ^
  --ocs-transform log ^
  --image-size 128 ^
  --intensity log1p ^
  --split 10to5 ^
  --batch-size 32 ^
  --epochs 500 ^
  --patience 100 ^
  --lr 1e-3 ^
  --weight-decay 1e-4 ^
  --dropout 0.10 ^
  --seeds 0 1 2 3 4 ^
  --num-workers 0 ^
  --device auto ^
  --tag per_part_log
```

---

### 5. total_log × 5 seeds

```bat
python -u train_fusion.py ^
  --ocs-root "%OCS_ROOT%" ^
  --scan-json "%SCAN_JSON%" ^
  --image-dir "%IMAGE_DIR%" ^
  --out-root "%OUT_ROOT%" ^
  --geom-set concat5 ^
  --ocs-feat total ^
  --ocs-transform log ^
  --image-size 128 ^
  --intensity log1p ^
  --split 10to5 ^
  --batch-size 32 ^
  --epochs 500 ^
  --patience 100 ^
  --lr 1e-3 ^
  --weight-decay 1e-4 ^
  --dropout 0.10 ^
  --seeds 0 1 2 3 4 ^
  --num-workers 0 ^
  --device auto ^
  --tag total_log
```

---

## 输出要求

每个 run 输出到：

```text
结果/模块C_反演/cnn_ocs_fusion/run_YYYYMMDD_HHMMSS_<tag>/
```

至少包含：

```text
config_used.json
summary.json
summary.csv
metrics_seed0.json
...
predictions_seed0.csv
...
best_model_seed0.pt
...
training_curve_seed0.csv
...
```

---

## 结果表模板

完成后整理：

| Method | OCS input | Image input | split | mean | p90 | Hit5 | Hit10 |
|---|---|---|---|---:|---:|---:|---:|
| OCS-only MLP | all raw 45D | - | 10°→5° | 3.98±0.6° | 4.82° | 90.7% | 97.1% |
| CNN image-only | - | phase63 log1p 128² | 10°→5° | 12.38±0.74° | - | 26.1% | 55.8% |
| late fusion | all raw 45D | phase63 log1p 128² | 10°→5° | 5.03° | 待填 | 待填 | 待填 |
| feature fusion | all raw 45D | phase63 log1p 128² | 10°→5° | 待填 | 待填 | 待填 | 待填 |
| feature fusion | per_part log 30D | phase63 log1p 128² | 10°→5° | 待填 | 待填 | 待填 | 待填 |
| feature fusion | total log 15D | phase63 log1p 128² | 10°→5° | 待填 | 待填 | 待填 | 待填 |

---

## 判断标准

### all_raw

目标是超过或至少接近 OCS-only MLP：

```text
mean < 3.98° 或 Hit5 > 90.7%
```

如果没超过，也有结论：

```text
单视角图像对 all_raw semi-oracle OCS 互补有限。
```

### per_part_log

目标：

```text
mean < 5.91°
Hit5 > 73.8%
```

### total_log

这是最重要的实际意义 case。

目标：

```text
显著优于 total_log OCS-only MLP mean=36.69°
并优于 CNN-only mean=12.38°
```

如果 total_log fusion < 12.38°，说明弱 OCS 总量特征确实能补充图像。

---

## 暂停条件

完成 3 个 feature-level fusion case 的 5 seeds 实验，并更新 `CLAUDE.md` / `result.md` 后暂停。

暂时不要继续：

```text
更大 CNN
224 分辨率
数据增强
多相位角图像
roll 轴扩展
```

等 feature-level 结果出来后再决定。