# A2_Codex复审

复审时间：2026-06-09

复审对象：

```text
06_Claude执行方案/Claude输出/A2_亮度范围调研整理.md
01_调研_常规卫星光度范围/卫星光度调研表.csv
```

## 总体结论

**A2 复审通过，可以并入主成果区，并进入 A3。**

本轮修订已经解决前次审阅指出的核心问题：

1. `卫星光度调研表.csv` 新增了 `doi_or_url` 和 `use_for_A9_sanity_check` 两列。
2. 旧阶段3证据体系已落表为 19 条记录：E1-E18，含 E5b 补充条目。
3. CSV 可正常解析，A9 分层字段有效：
   - `yes`: 6 条核心直接证据；
   - `indirect`: 7 条间接证据；
   - `no`: 6 条 glint/碎片/inactive/背景证据。
4. A2 文档已将 V 波段和 `m_sun(V) ≈ -26.74` 改为 A9 候选口径，而不是已锁定口径。
5. 证据分层已经区分 active GEO 直接证据、间接系统边界证据、glint 机制证据和背景证据。

## 复审检查

| 检查项 | 结论 | 说明 |
|---|---|---|
| CSV 字段是否补齐 | 通过 | 已有 `doi_or_url` 和 `use_for_A9_sanity_check` |
| CSV 证据数量是否闭合 | 通过 | 19 条记录，对应 E1-E18 含 E5b |
| A9 sanity check 分层是否有效 | 通过 | `yes/indirect/no` 三类可解析 |
| 是否仍从零重做文献调研 | 通过 | 复用旧阶段3，不新增检索 |
| 是否过度声称 V 波段/太阳星等已锁定 | 通过 | 已改为候选口径 |
| 是否反向证明 OCS 模型正确 | 通过 | 明确只作外部范围参考 |
| 是否可进入 A3 | 通过 | A2 任务闭合 |

## 通过后处理

A2 正式文件应移动到：

```text
01_调研_常规卫星光度范围/A2_亮度范围调研整理.md
```

`06_Claude执行方案/Claude输出/` 不长期保留已通过成果。

## 给 A3 的衔接要求

A3 可以开始几何搜索空间设计，但需继承 A1/A2 的边界：

- `range_km` 只作为星等后处理元数据，不进入 OCS 积分。
- 几何字段必须记录相位角、太阳方向、观测方向和距离/场景口径。
- A2 的 11-15 mag 只能作为 A9 情景估计 sanity check，不参与 A3 的 OCS 搜索目标函数。
- V 波段和 `m_sun(V) ≈ -26.74` 只是 A9 候选口径，不在 A3 中固化为最终标定。
