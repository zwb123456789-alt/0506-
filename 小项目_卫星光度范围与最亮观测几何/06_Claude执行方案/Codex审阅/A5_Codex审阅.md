# A5_Codex审阅

最后更新：2026-06-09

## 审阅对象

```text
06_Claude执行方案/Claude输出/A5_脚本与输出规范.md
06_Claude执行方案/Claude输出/A5_输出字段规范表.csv
```

## 审阅结论

**通过，允许进入 A6。**

A5 已经把 A4 的 S1-S5 搜索路线转换为脚本边界、输入输出、字段、口径追踪和结果归档规则。审阅中发现一处会影响 A6 落地的问题：草稿最初假设小项目归档区已有旧五几何 `ocs_scan.csv`，但实际当前小项目内只固定了 phase63 三份旧输出。该问题已修正。

## 已修正的问题

| 问题 | 修正 |
|---|---|
| A5 将 `phase63_*.csv` 写成“旧五几何预实验结果” | 改为“当前已固定到小项目内的 phase63 预实验结果” |
| A6 输入假设存在 phase24/45/63/90/120 的 `ocs_scan.csv` | 改为 A6-M0 先做证据库存；旧五几何扩展复核必须先定位并复制证据 |
| A6 可能基于 roll=0 结果判断 roll 不敏感 | 明确 A6 不能判断 roll 不敏感；roll 验证留给 A7 或作为未验证限制 |
| A6 输出过早写成旧五几何汇总 | 改为 phase63 最小复核为必做，旧五几何汇总为证据定位后的可选扩展 |

## 通过依据

1. **保护规则合格**：脚本和结果只允许写在小项目内；原大项目代码、STL、旧结果只读调用。
2. **路径规则合格**：没有重新启用旧 `04_STL实验判断/` 作为活跃输出目录；未审阅运行结果先放 `06_Claude执行方案/Claude输出/Ax_*_运行草稿/`。
3. **字段规范合格**：主文档和 CSV 字段表覆盖几何、姿态、OCS、部件占比、遮挡、追溯、机制和 Delta m 字段。
4. **口径追踪合格**：明确 `face-center (legacy)`、`pixel-level (v0.4)` 和 `mixed (caution)`，并禁止混表排序。
5. **half-vector 边界合格**：`is_near_halfvector` 在法向不足时为 null/unknown，不强行判定。
6. **A6 入口合格**：下一步从证据库存和 phase63 最小复核开始，避免直接假设旧五几何文件齐全。

## 残余风险

| 风险 | A6处理方式 |
|---|---|
| phase24/45/90/120 原始结果尚未固定到小项目内 | A6 先做证据库存；能定位则复制到小项目 evidence/ 并记录来源，不能定位则只做 phase63 最小复核 |
| 旧 CSV 字段名可能与 A5 规范不一致 | A6 做字段映射，并把缺失字段标注为 null/not_recorded |
| A6 只有 roll=0 证据 | A6 不判断 roll 敏感性，不排除 roll 峰值 |
| A6 仍是旧口径预实验 | 结果只能作为 face-center legacy 内部相对排序和候选来源 |

## 正式归档决定

将 A5 主文档与字段表正式并入：

```text
00_A0-A10正式成果区/A5_脚本与输出规范/
```

正式文件：

```text
A5_脚本与输出规范.md
A5_输出字段规范表.csv
```

## 下一步

进入 **A6：旧预实验复核**。

A6 的第一步不是直接宣称完成旧五几何复核，而是：

1. 做证据库存；
2. 对已固定 phase63 三份输出做最小复核；
3. 若定位到旧五几何原始结果，再复制证据并扩展复核；
4. 所有结果先放入 `06_Claude执行方案/Claude输出/A6_旧五几何预实验复核_运行草稿/`，通过审阅后再进入正式成果区。
