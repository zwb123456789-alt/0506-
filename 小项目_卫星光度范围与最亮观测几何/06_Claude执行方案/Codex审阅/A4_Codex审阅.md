# A4_Codex审阅

最后更新：2026-06-09

## 审阅对象

```text
06_Claude执行方案/Claude输出/A4_三维姿态搜索方案.md
```

## 审阅结论

**通过，允许进入 A5。**

A4 已经完成从 A3 几何空间到三维姿态搜索的衔接，核心价值是把后续搜索拆成可执行的 S1-S5：

1. 旧结果候选提取；
2. 代表几何 yaw/pitch 粗扫；
3. 候选姿态 roll 扫描；
4. 局部三维细化；
5. 机制标注。

该方案没有把旧 phase63、roll=0 的结果误写成全局最亮，也没有把 OCS 当成绝对视星等。A4 的角色仍是搜索方案，不是最终物理结论。

## 已修正的问题

| 问题 | 修正 |
|---|---|
| 输出路径仍指向旧 `04_STL实验判断/outputs_v2/` | 已改为不得重新启用旧归档目录；未审阅结果先留在 `06_Claude执行方案/Claude输出/`，通过后进入 `00_A0-A10正式成果区/` 对应阶段 |
| “3 个代表几何都类似即可推断其他几何也类似”表述过强 | 已改为只能作为降低其他相位角优先级的依据，不能直接外推；必要时 A7 扩展 |
| `is_near_halfvector` 默认可判定 | 已改为 boolean/null；若缺少部件或主导面法向数据，必须记为 null/unknown，不得强行判定 |

## 通过依据

1. **保护规则合格**：A4 只读取旧支撑材料，不修改原大项目文件；修订后的输出路径不再指向旧活跃目录。
2. **边界合格**：旧 phase63 只作为 roll=0 预实验与候选来源，不作为真实高轨/GEO 全局最亮结论。
3. **搜索策略可执行**：分级搜索避免了 12 个几何 × yaw/pitch/roll 全扫描的组合爆炸。
4. **候选来源合理**：不仅取总 OCS top-K，也纳入金属主体、太阳能板 OCS、太阳能板占比和旧 phase63 top-20。
5. **roll 维度补齐**：明确全周期 roll 扫描优先，只有验证对称性后才考虑半周期。
6. **机制字段足够支撑 A8**：包含主导部件、贡献占比、遮挡率、roll 敏感度、峰宽和 half-vector 条件。

## 残余风险

| 风险 | 处理方式 |
|---|---|
| 代表几何只选 0/63/120，可能遗漏某些相位角峰值 | A5 保留可配置几何集合；A7 根据 A6/A5 结果决定是否扩展 |
| 分离式局部细化可能漏掉 yaw/pitch/roll 耦合峰 | A5 需要同时给出分离扫描和局部笛卡尔/自适应扫描接口 |
| half-vector 判据依赖法向数据质量 | A5 必须把该字段设为可空，并记录法向来源 |
| A4 仍是方案，不是实施结果 | 下一阶段 A5 必须进入脚本/字段/输出路径的硬规范，A6 开始实际复核 |

## 正式归档决定

将修订后的 A4 正式并入：

```text
00_A0-A10正式成果区/A4_三维姿态搜索方案/A4_三维姿态搜索方案.md
```

`06_Claude执行方案/Claude输出/` 不长期保留通过版 A4，只保留 Claude/Codex 对话和后续草稿。

## 下一步

进入 **A5：脚本与输出规范**。

A5 不再继续泛泛规划，必须把 A4 的搜索方案落成可执行规范：脚本边界、输入输出文件、字段名、阶段目录、口径标注、结果审阅路径，以及 A6 实际复核如何运行。
