# 阶段4 Codex复审：高轨几何到 OCS 接口设计

复审时间：2026-06-07

审阅对象：

- `Claude输出/阶段4_高轨几何到OCS接口设计.md`

## 复审结论

**通过，可以进入阶段5。**

上一轮 Codex 审阅提出的两个必改问题已经修正：

1. `sun_direction_I` / `det_direction_I` 的物理方向已锁死；
2. 输出目录已从原大项目 `结果/模块A_重构` 改为小项目内部目录。

阶段4现在可以作为阶段5包装层设计/实施的接口依据。

## 必改项复核

### 1. 方向向量物理含义：已通过

文档新增了 `§2.3 方向向量物理含义（关键约定）`，明确：

```text
sun_direction_I = normalize(r_sun_I - r_sat_I)
det_direction_I = normalize(r_obs_I - r_sat_I)
range_km = ||r_obs_I - r_sat_I||
```

并写清：

```text
sun_direction_I = 卫星 → 太阳
det_direction_I = 卫星 → 观测者
range_km = 卫星到观测者斜距
```

这与现有 `ocs_core.py` 的可见面筛选逻辑一致。

### 2. 输出目录保护规则：已通过

文档 `§6.3 目录结构` 已改为：

```text
小项目_卫星光度范围与最亮观测几何/07_阶段4输出_高轨OCS接口/{mission_name}/
```

并明确：

```text
可以只读调用原大项目代码和 STL 文件；
不得向原大项目 结果/模块A_重构 写入任何新文件或目录；
所有输出落在小项目文件夹内。
```

这符合用户此前的原大项目保护规则。

## 建议项复核

### 1. `range_km` 斜距说明：基本通过

输入 schema 中已将示例改为 `38000.0`，并明确 `range_km` 是 slant range，不是轨道高度。

小残留：输出 JSON/CSV 示例中仍有少量 `range_km: 36000.0 / 36010.0`。这不影响接口设计，但建议阶段5实施前统一为 `38000.0 / 38010.0`，或在示例旁标注“近似斜距示例”。

### 2. 相位角定义：已通过

`§7.3` 已明确：

```text
phase_angle_deg = arccos(dot(sun_direction_I, det_direction_I))
```

且两向量均以卫星为起点。

### 3. 阶段3范围对照：已通过

`§10` 已将“是否落入 11-15 mag”改为 sanity check，并明确若偏离，不直接判定代码错误，而是检查 STL 尺度、BRDF 参数、精度抽稀、相位角、姿态和星等换算假设。

### 4. 输出字段重命名：已通过

`§4.1` 已说明 `_m2` 和 `_percent` 字段由包装层从现有代码返回值转换/重命名得到，核心函数返回字段保持不变。

## 阶段5前置锁定项

阶段5实施前必须继续保持以下约束：

```text
sun_direction_I = 卫星 → 太阳
det_direction_I = 卫星 → 观测者
range_km = 卫星到观测者斜距
所有新增输出写入小项目文件夹，不写入原大项目结果目录
不修改 ocs_core.py / geometry.py / config.py 核心逻辑
```

## 最终建议

阶段4可以封存为：

```text
阶段4：通过，可进入阶段5。
```

阶段5可以开始设计包装层或执行方案，但应优先做一个极简单时刻 JSON 测试，验证方向符号、相位角、输出路径和字段转换。

