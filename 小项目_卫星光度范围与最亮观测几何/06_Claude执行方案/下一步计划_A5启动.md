# 下一步计划_A5启动

最后更新：2026-06-09

## 当前状态

A0-A4 已通过 Codex 审阅并进入正式成果区。当前项目不再停留在“是否要做三维姿态搜索”的讨论，而是进入 **A5：脚本与输出规范**。

## A5目标

A5 的任务是把 A4 的 S1-S5 搜索方案转换成可执行、可审阅、可复用的脚本与数据规范。

A5 仍然不是正式运行阶段，但必须为 A6 的实际复核铺好接口。A5 之后，A6 要开始产出真实复核表、top-K 表、per-part 贡献表和 Delta m 表。

## A5必须回答的问题

1. 小项目内应放哪些脚本草案或脚本说明，是否新建脚本目录？
2. 每个搜索阶段 S1-S5 的输入文件、输出文件、字段、命名规则是什么？
3. 如何只读调用原大项目代码/STL/旧结果，而不向原大项目写入？
4. 如何区分 `face-center (legacy)` 与可能的 `pixel-level` 口径？
5. 如何记录几何、姿态、材料/部件、遮挡、OCS、Delta m、机制标签？
6. 未审阅运行结果、通过结果、正式成果分别放哪里？
7. A6 旧五几何预实验复核需要先做哪一组最小可运行输出？

## 建议输出

Claude 输出到：

```text
06_Claude执行方案/Claude输出/A5_脚本与输出规范.md
```

如需列字段，可在同一目录输出：

```text
06_Claude执行方案/Claude输出/A5_输出字段规范表.csv
```

如需给出脚本结构草案，可先写成文档，不急着写真实代码：

```text
06_Claude执行方案/Claude输出/A5_脚本结构草案.md
```

## 通过后正式位置

```text
00_A0-A10正式成果区/A5_脚本与输出规范/
```

## A5通过条件

1. 不再指向已归档旧 `04_STL实验判断/` 作为活跃输出目录。
2. 明确 A6/A7 运行结果在审阅前和通过后的路径。
3. 明确所有必要字段，尤其是 geometry、attitude、per-part OCS、occlusion、data_source、candidate_source、mechanism_label。
4. 明确哪些字段可以为空，例如 `is_near_halfvector` 在缺少法向数据时为 null/unknown。
5. 明确 A6 的最小实施单元，避免继续只写计划。

## 下一步给Claude

使用：

```text
06_Claude执行方案/Claude提示词/A5_脚本与输出规范.md
```
