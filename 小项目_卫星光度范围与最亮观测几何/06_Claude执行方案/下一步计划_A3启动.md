# 下一步计划：A3 启动

生成时间：2026-06-09

## 当前状态

A2 已通过 Codex 复审，并完成正式并入：

```text
00_A0-A10正式成果区/A2_亮度范围调研/A2_亮度范围调研整理.md
00_A0-A10正式成果区/A2_亮度范围调研/卫星光度调研表.csv
```

A2 审阅记录保留在：

```text
06_Claude执行方案/Codex审阅/A2_Codex审阅.md
06_Claude执行方案/Codex审阅/A2_Codex复审.md
```

## 当前下一步

执行 **A3：几何搜索空间设计**。

A3 是方案设计阶段，不运行仿真，不写脚本，不输出最终最亮结论。

## A3 目标

1. 设计太阳-目标-观测几何搜索空间。
2. 明确相位角、太阳方向、观测方向、距离/场景元数据字段。
3. 继承 A1 的约束：
   - `range_km` 只用于星等后处理，不进入 OCS 积分；
   - OCS 搜索目标仍是相对 OCS、部件贡献和机制归纳；
   - 不跨 face-center 与 pixel-level 口径混表。
4. 继承 A2 的边界：
   - 11-15 mag 仅作为 A9 情景星等估计 sanity check；
   - V 波段和 `m_sun(V) ≈ -26.74` 只是 A9 候选口径；
   - 不用文献亮度范围反向约束 OCS 搜索。
5. 输出几何搜索空间设计文档和必要的矩阵草案。

## Claude 输出位置

```text
06_Claude执行方案/Claude输出/A3_几何搜索空间设计.md
```

如需更新几何矩阵，只能更新或新增在：

```text
06_Claude执行方案/Claude输出/
```

建议输出：

```text
06_Claude执行方案/Claude输出/A3_几何搜索空间设计.md
06_Claude执行方案/Claude输出/A3_几何搜索空间矩阵_草案.csv
```

通过 Codex 审阅后，再移入：

```text
00_A0-A10正式成果区/A3_几何搜索空间设计/
```

## A3 通过标准

1. 几何字段定义清楚：`phase_angle_deg`、`sun_direction_I`、`det_direction_I`、`range_km`。
2. 方向向量语义清楚：
   - `sun_direction_I` = 卫星指向太阳；
   - `det_direction_I` = 卫星指向观测者。
3. 相位角覆盖策略合理，不只沿用旧五几何。
4. 明确旧五几何只是预实验，不等于真实高轨/GEO完整几何。
5. 不把距离、星等或 A2 亮度范围放进 OCS 积分目标。
6. 为 A4 姿态搜索提供可执行但不过度复杂的几何候选集合。
