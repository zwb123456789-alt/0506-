# 下一步计划_A7启动

最后更新：2026-06-09

## 当前状态

A0-A6 已通过 Codex 审阅并进入正式成果区。当前进入 **A7：新几何与 roll 实施计划**。

A6 已经完成第一步具体复核：phase63 证据库存和标准化结果已经可用；旧五几何完整原始 CSV 已定位但未复制到小项目内。

## A7目标

A7 要做的是实施决策，而不是直接把 A6 写成最终机制结论。

重点回答：

1. 是否需要复制旧五几何完整 `ocs_scan.csv` 到小项目 evidence/？
2. 是否需要对 phase63 top-K 候选做 roll 扫描？
3. 是否需要扩展到 A3 的代表几何 phase0/63/120？
4. 如果暂不实施 roll 或新几何，必须在 A8/A10 写入什么限制？
5. 需要多少计算量、哪些脚本、哪些输出字段和哪些审阅路径？

## A7必须继承的边界

- A6 只有 phase63、roll=0、face-center legacy 的最小复核。
- A6 不能判断 roll 不敏感。
- A6 不能排除太阳能板 roll glint。
- A6 不能作为最终 OCS 数值结论。
- 旧五几何完整 CSV 只是已定位，尚未复制固定到小项目。

## 建议输出

Claude 主文档：

```text
06_Claude执行方案/Claude输出/A7_新几何与roll实施计划.md
```

若需要列计算预算，可另输出：

```text
06_Claude执行方案/Claude输出/A7_计算预算表.csv
```

若决定需要运行，先输出运行计划，不直接大规模运行：

```text
06_Claude执行方案/Claude输出/A7_新几何与roll实施_运行草稿/
```

## A7通过条件

1. 明确给出“实施 / 暂不实施 / 分阶段实施”的判断。
2. 若暂不实施 roll，必须写清 roll 未验证的限制。
3. 若实施 roll，必须给出候选集、roll 步长、输出字段和计算预算。
4. 若复制旧五几何完整 CSV，必须复制到小项目内并记录来源路径。
5. 不向小项目外写入任何文件。

## 下一步给Claude

使用：

```text
06_Claude执行方案/Claude提示词/A7_新几何与roll实施计划.md
```
