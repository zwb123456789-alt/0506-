# 下一步计划：A0 启动

生成时间：2026-06-09

## 当前状态

小项目文件结构已调整完成：

- `06_Claude执行方案/` 仅作为 Claude/Codex 对话区。
- Claude 提示词放入 `06_Claude执行方案/Claude提示词/`。
- Claude 草稿输出放入 `06_Claude执行方案/Claude输出/`。
- Codex 审阅放入 `06_Claude执行方案/Codex审阅/`。
- 审阅通过的正式成果再放回 `01-05/` 主文件夹对应位置。

旧阶段核心成果已经回到主成果区，旧阶段中间过程仍保留在 `Claude输出/_旧阶段归档_中间过程/`。

## 当前下一步

执行 **A0：边界冻结与旧结果体检**。

A0 只做边界与旧结果证据等级确认，不运行仿真，不做新结论，不写论文导向叙事。

## A0 目标

1. 冻结小项目只有两个任务：
   - 调研常规高轨/GEO类卫星亮度或视星等范围。
   - 基于 STL、材料 BRDF、遮挡和 OCS 链路，寻找最亮太阳-目标-观测几何、卫星姿态和材料/部件机制。
2. 明确第二任务不是单点 OCS 最大值，而是解释最亮构型族：
   - 什么材料/部件最亮；
   - 在什么太阳-观测几何与 yaw/pitch/roll 姿态下最亮；
   - 最亮来自镜面角平分、受照可见面积、遮挡最少，还是材料参数。
3. 体检旧 phase63 与五几何结果的证据边界：
   - 只能作为预实验；
   - 不能称为真实高轨/GEO全局最亮结论；
   - 不能直接给出真实绝对视星等。
4. 给出进入 A1 前的判断：
   - 是否必须补 roll；
   - 是否必须扩展多几何；
   - 是否需要局部细化；
   - 情景绝对星等估计应推迟到 A9。

## Claude 输出位置

Claude 完成 A0 后，输出到：

```text
06_Claude执行方案/Claude输出/A0_边界与旧结果体检.md
```

## Codex 审阅位置

Claude 输出 A0 后，Codex 审阅写入：

```text
06_Claude执行方案/Codex审阅/A0_Codex审阅.md
```

## 是否可进入 A1 的判据

A0 通过后才能进入 A1。通过标准：

1. 没有违反原大项目保护规则。
2. 没有把小项目写成论文附属任务。
3. 没有把旧 phase63 或五几何预实验说成真实全局结论。
4. 没有混淆 OCS、相对星等差 Delta m 和绝对视星等。
5. 明确后续需要从采样逻辑、roll、多几何和机制归纳角度推进。
