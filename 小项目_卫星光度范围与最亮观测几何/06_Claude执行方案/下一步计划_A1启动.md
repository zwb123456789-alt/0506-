# 下一步计划：A1 启动

生成时间：2026-06-09

## 当前状态

A0 已完成并通过 Codex 审阅。

A0 审阅文件：

```text
06_Claude执行方案/Codex审阅/A0_Codex审阅.md
```

A0 结论：可以进入 A1，但 A1 必须吸收以下修正要求：

- 不给 OCS 强行加单位，除非完成定义审计。
- 不使用“论文可用”“论文结论”等论文导向措辞。
- 不固定 roll 扫描角度数量；具体步长和预算留给 A4。
- 所有旧结果继续标注为预实验或旧口径证据。
- 统一使用 A0-A10 阶段编号，不再使用旧 Phase 1/2a。

## 当前下一步

执行 **A1：OCS定义与采样逻辑审计**。

A1 仍然是审计/方案阶段，不运行仿真，不生成新脚本，不输出最终最亮结论。

## A1 目标

1. 审计当前 OCS 的定义、单位口径和可用边界。
2. 明确相对星等差 `Delta m = -2.5 log10(OCS_2 / OCS_1)` 的可用条件。
3. 明确绝对视星等必须推迟到 A9 做情景估计，不能在 A1 冒充实测。
4. 审计旧采样逻辑：
   - yaw/pitch 欧拉网格问题；
   - yaw=0/360 重复问题；
   - roll=0 固定导致漏扫 glint 的问题；
   - 五几何 phase24/45/63/90/120 不是完整高轨/GEO几何空间；
   - 旧五几何 face-center 口径与后续 pixel-level 口径不能直接混表。
5. 为 A3/A4/A5 给出清晰约束，而不是提前实施。

## Claude 输出位置

```text
06_Claude执行方案/Claude输出/A1_OCS与采样逻辑审计.md
```

## Codex 审阅位置

```text
06_Claude执行方案/Codex审阅/A1_Codex审阅.md
```

## 是否可进入 A2/A3 的判据

A1 通过后：

- A2 可基于旧阶段3成果做格式对齐与 csv 落表，不重做文献调研。
- A3 可开始几何搜索空间设计。
- A4 可在 A3/A1 约束下设计 yaw/pitch/roll 粗到细策略。

A1 未通过时，不应进入几何/姿态搜索设计。
