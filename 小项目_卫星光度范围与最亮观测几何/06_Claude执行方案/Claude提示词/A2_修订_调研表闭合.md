# 给 Claude 的提示词：A2 修订-调研表闭合

请只修订 A2，不要进入 A3，不要新增仿真，不要从零重做文献检索。

## 必读文件

请读取：

```text
06_Claude执行方案/Claude输出/A2_亮度范围调研整理.md
06_Claude执行方案/Codex审阅/A2_Codex审阅.md
01_调研_常规卫星光度范围/阶段3_常规卫星光度范围调研.md
01_调研_常规卫星光度范围/卫星光度调研表.csv
```

## 本次修订目标

使 A2 达到可通过 Codex 审阅、可并入 `01_调研_常规卫星光度范围/` 的状态。

## 必须修订

### 1. 更新 CSV 字段

请更新：

```text
01_调研_常规卫星光度范围/卫星光度调研表.csv
```

至少新增两列：

```text
doi_or_url
use_for_A9_sanity_check
```

填表规则：

- 已在旧阶段3明确给出 DOI/URL 的，填对应 DOI/URL。
- 未明确给出的，填 `not_reported` 或 `unknown`。
- `use_for_A9_sanity_check` 只使用：
  - `yes`：可作为 A9 常规亮度范围 sanity check 的直接证据；
  - `indirect`：探测边界、混合群体、碎片背景等间接参考；
  - `no`：LEO 对照、纯 glint 机制、中文背景仿真或非 GEO 常规亮度证据。

### 2. 闭合 18 条证据与 CSV 行数关系

A2 当前声称复用旧阶段3的 E1-E18，但 CSV 只有 10 行。请修订为以下两种之一：

**优先方案**：将旧阶段3 E1-E18 全部落入 CSV，使 CSV 成为 A2 完整调研表。

**保守方案**：如果无法完整落表，必须在 A2 文档中明确说明当前 CSV 仍未覆盖 E1-E18，A2 不能声称调研表完整闭合。

推荐采用优先方案。

### 3. 修订 A2 文档表述

请修订：

```text
06_Claude执行方案/Claude输出/A2_亮度范围调研整理.md
```

必须改：

- 不要说 A2 已“锁定”V 波段和太阳星等。改为：A2 建议 A9 优先采用 V 波段情景口径，`m_sun(V) ≈ -26.74` 作为候选值，最终由 A9 确认。
- 删除“论文级引用”“论文终稿”等论文导向措辞，改为“正式引用”“正式结论”。
- 明确分层：
  - 直接 active GEO 证据；
  - 间接系统边界证据；
  - glint 机制证据；
  - 背景/不用于 A9 对照证据。

### 4. 输出修订说明

在 A2 文档中新增或更新一节：

```text
## 本轮修订说明
```

说明：

- CSV 新增了哪些字段；
- CSV 当前共有多少条记录；
- 是否已经覆盖 E1-E18；
- 哪些记录可用于 A9 sanity check；
- 哪些记录只能作为间接或背景证据。

## 保护规则

- 不删除任何文件。
- 不修改小项目外任何文件。
- 不新增小项目外任何文件。
- 不运行仿真。
- 不编造 DOI、URL、波段、距离或星等范围。

## 完成后

请保持 A2 草稿仍在：

```text
06_Claude执行方案/Claude输出/A2_亮度范围调研整理.md
```

等待 Codex 复审。通过后 Codex 会将正式版并入：

```text
01_调研_常规卫星光度范围/A2_亮度范围调研整理.md
```
