# A6_旧预实验复核_给Claude提示词

请继续小项目，不要回到大项目论文主线。

## 必读文件

只读取以下文件恢复上下文：

1. `00_小项目总览.md`
2. `00_原大项目文件保护规则.md`
3. `00_A0-A10正式成果区/README.md`
4. `00_A0-A10正式成果区/A1_OCS定义与采样逻辑审计/A1_OCS与采样逻辑审计.md`
5. `00_A0-A10正式成果区/A5_脚本与输出规范/A5_脚本与输出规范.md`
6. `00_A0-A10正式成果区/A5_脚本与输出规范/A5_输出字段规范表.csv`

## 本阶段任务

完成 **A6：旧预实验复核**。

A6 是第一步具体实施，不要继续只写计划。请先做证据库存，再对当前已固定的 phase63 三份输出做最小复核。

## 已固定的phase63证据

当前小项目内已固定：

```text
90_旧结构与支撑材料归档/04_STL实验判断/outputs/phase63_topk_brightness.csv
90_旧结构与支撑材料归档/04_STL实验判断/outputs/phase63_component_contribution.csv
90_旧结构与支撑材料归档/04_STL实验判断/outputs/phase63_delta_mag_summary.md
```

这些是 `face-center (legacy)` 旧口径结果，只能作为预实验复核和候选来源。

## 旧五几何证据规则

不要假设小项目内已经有 phase24/45/90/120 的 `ocs_scan.csv`。

若要扩展到旧五几何，请先只读搜索/定位原大项目中的 phase24/45/63/90/120 原始结果文件。若需要固定证据，只能复制到小项目内：

```text
06_Claude执行方案/Claude输出/A6_旧五几何预实验复核_运行草稿/evidence/
```

并在 A6 文档和证据库存表中记录来源路径。

## 输出位置

Claude 主文档：

```text
06_Claude执行方案/Claude输出/A6_旧五几何预实验复核.md
```

运行草稿目录：

```text
06_Claude执行方案/Claude输出/A6_旧五几何预实验复核_运行草稿/
```

建议结构：

```text
06_Claude执行方案/Claude输出/A6_旧五几何预实验复核_运行草稿/
├── evidence/
├── outputs/
└── scripts/
```

## 必须生成的最小输出

```text
06_Claude执行方案/Claude输出/A6_旧五几何预实验复核_运行草稿/outputs/a6_evidence_inventory.csv
06_Claude执行方案/Claude输出/A6_旧五几何预实验复核_运行草稿/outputs/a6_phase63_topk_standardized.csv
06_Claude执行方案/Claude输出/A6_旧五几何预实验复核_运行草稿/outputs/a6_phase63_component_standardized.csv
06_Claude执行方案/Claude输出/A6_旧五几何预实验复核_运行草稿/outputs/a6_phase63_delta_mag_summary.md
```

可选输出（仅当旧五几何证据被定位并复制后）：

```text
outputs/a6_five_geom_topk.csv
outputs/a6_five_geom_component_max.csv
outputs/a6_five_geom_delta_mag.csv
```

## 字段要求

统一字段必须参考：

```text
00_A0-A10正式成果区/A5_脚本与输出规范/A5_输出字段规范表.csv
```

至少补充：

```text
geometry_label
phase_angle_deg
yaw_deg
pitch_deg
roll_deg
total_ocs_model
ocs_jinshuzhuti_model
ocs_taiyangnengban_model
ocs_yinshenban_model
pct_jinshuzhuti
pct_taiyangnengban
pct_yinshenban
occlusion_ratio_percent
data_source
search_stage
source_file
script_name
run_id
is_near_halfvector
```

若旧数据缺少某字段，填 `null` 或 `not_recorded`，不要编造。

`data_source` 固定为：

```text
face-center (legacy)
```

`is_near_halfvector` 固定为：

```text
null 或 unknown
```

## A6禁止事项

- 不重新运行 OCS。
- 不做 roll 扫描。
- 不做局部细化。
- 不判断 roll 不敏感。
- 不把 phase63 roll=0 称为三维姿态全局最亮。
- 不把 A6 旧口径结果作为小项目最终结论。
- 不向小项目外写入任何文件。
- 不修改、删除、移动、覆盖原大项目文件。

## A6文档必须包含

1. 本阶段目标
2. 读取的原项目文件
3. 写入的小项目文件
4. 证据库存
5. phase63 最小复核结果
6. 若定位到旧五几何，说明来源路径和复制路径
7. 字段映射说明
8. 不能支持的结论
9. 风险与待确认
10. 给 Codex 的审阅问题
11. 下一阶段建议

## 审阅重点

Codex 会重点审阅：

1. 是否真的产出了证据库存和 phase63 复核表。
2. 是否误称旧五几何证据已经齐全。
3. 是否把 roll=0 结果写成 roll 不敏感。
4. 是否违反小项目外写入保护规则。
5. 是否把 face-center 旧口径写成最终结论。
