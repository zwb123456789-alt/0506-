# 给 Claude 的提示词：A2 亮度范围调研整理

请严格执行 A2。A2 不是从零重做文献调研，而是复用旧阶段3成果，做格式对齐、字段核查和 csv 落表。

## 必读文件

请读取：

```text
00_小项目总览.md
00_原大项目文件保护规则.md
06_Claude执行方案/00_Claude执行总览.md
06_Claude执行方案/README.md
A0_边界与旧结果体检.md
02_OCS_光度换算标准/A1_OCS与采样逻辑审计.md
06_Claude执行方案/Codex审阅/A1_Codex审阅.md
01_调研_常规卫星光度范围/阶段3_常规卫星光度范围调研.md
01_调研_常规卫星光度范围/卫星光度调研表.csv
01_调研_常规卫星光度范围/文献笔记.md
```

## 保护规则

- 不删除、修改、移动、重命名、覆盖小项目文件夹之外的任何原大项目文件。
- 不向小项目文件夹之外添加任何新文件。
- 不运行仿真。
- 不生成脚本。
- 可以更新小项目内的调研表和说明文档。

## 输出位置

请将 A2 草稿说明写入：

```text
06_Claude执行方案/Claude输出/A2_亮度范围调研整理.md
```

如需更新 csv，只能更新：

```text
01_调研_常规卫星光度范围/卫星光度调研表.csv
```

## 本阶段目标

完成 A2：亮度范围调研整理。

请重点完成：

1. 复用旧阶段3，不重做、不扩大。
2. 检查调研表字段是否包含：
   - `source_id`
   - `citation`
   - `doi_or_url`
   - `object_or_satellite_type`
   - `orbit_context`
   - `band_or_filter`
   - `magnitude_or_brightness_range`
   - `observation_geometry_or_conditions`
   - `range_or_distance_context`
   - `evidence_level`
   - `use_for_A9_sanity_check`
   - `notes`
3. 如果当前 csv 字段不足，请在小项目内更新 csv 表头和已有记录，不要编造缺失信息；缺失处用 `unknown` 或 `not_reported`。
4. 总结常规高轨/GEO类卫星可见光亮度或视星等范围，标注证据等级。
5. 明确 A2 调研结果只作为外部观测范围参考和 A9 情景估计 sanity check，不反向证明 OCS 模型正确。

## 输出格式

请严格使用以下结构：

```text
# A2_亮度范围调研整理

## 本阶段目标

## 读取的原项目文件

只列路径和用途，不修改。

## 写入或更新的小项目文件

## 旧阶段3复用说明

## 调研表字段核查

## 常规高轨/GEO类卫星亮度范围结论

## 可用于A9情景估计对照的证据

## 不能支持的结论

## 风险与待确认

## 给 Codex 的审阅问题

## 下一阶段建议
```

## 特别禁止

- 不要从零重做文献调研。
- 不要编造 DOI、观测波段、距离或星等范围。
- 不要声称调研范围证明当前 OCS 仿真正确。
- 不要把 A2 写成绝对星等换算阶段；绝对星等估计留到 A9。
