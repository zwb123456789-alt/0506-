# 阶段1 Claude提示词：OCS定义与星等公式审计

请只完成审计与方案，不实施仿真、不改原大项目文件。

## 任务目标

判断当前项目中的 OCS 是否能严谨换算为光度/视星等，并明确哪些公式可用、哪些公式待确认。

## 需要审查的对象

优先阅读并摘要，不修改：

```text
ocs_project/01_code/ocs_core.py
结果/模块A_重构/multi_geom_ggx_yaw73_pitch37/run_20260527_195122/phase63_backscatter/config_used.json
结果/模块A_重构/multi_geom_ggx_yaw73_pitch37/run_20260527_195122/phase63_backscatter/ocs_scan.csv
```

## 必须回答

1. 当前 OCS 积分的数学形式是什么？
2. OCS 输出更接近：
   - 物理绝对散射截面 `m^2`；
   - 方向性散射量；
   - 相对 OCS；
   - 其他。
3. 星等换算是否需要 `4π`、`π`、BRDF归一化因子或标定常数？
4. 哪个公式当前可以可靠使用：

```text
Delta m = -2.5 log10(OCS_2 / OCS_1)
```

5. 下面哪个绝对星等形式可以作为候选，条件是什么：

```text
m = m_sun - 2.5 log10(OCS / R^2)
m = m_sun - 2.5 log10(OCS / (4πR^2))
m = K_cal - 2.5 log10(OCS) + 5 log10(R)
```

## 输出要求

输出《阶段1_OCS定义与星等公式审计.md》，包含：

- 代码审计摘要；
- 公式可信度分级；
- 待确认项；
- 推荐后续换算口径；
- 不可使用的表述清单。

## 禁止

- 禁止直接宣布绝对星等公式已成立；
- 禁止改代码；
- 禁止生成新仿真结果。

## 交付物

保存为：

```text
06_Claude执行方案/Claude输出/阶段1_OCS定义与星等公式审计.md
```

## 交给 Codex 审阅时重点

- 是否正确区分相对星等与绝对星等；
- 是否识别单位、BRDF归一化、距离项问题；
- 是否给出过度确定结论。
