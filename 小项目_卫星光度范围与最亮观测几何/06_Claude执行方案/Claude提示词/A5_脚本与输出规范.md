# A5_脚本与输出规范_给Claude提示词

请继续小项目，不要回到大项目论文主线。

## 必读文件

只读取以下文件恢复上下文：

1. `00_小项目总览.md`
2. `00_原大项目文件保护规则.md`
3. `00_A0-A10正式成果区/README.md`
4. `00_A0-A10正式成果区/A1_OCS定义与采样逻辑审计/A1_OCS与采样逻辑审计.md`
5. `00_A0-A10正式成果区/A3_几何搜索空间设计/A3_几何搜索空间设计.md`
6. `00_A0-A10正式成果区/A4_三维姿态搜索方案/A4_三维姿态搜索方案.md`

如需旧结果证据，只读：

```text
90_旧结构与支撑材料归档/04_STL实验判断/outputs/phase63_topk_brightness.csv
90_旧结构与支撑材料归档/04_STL实验判断/outputs/phase63_component_contribution.csv
90_旧结构与支撑材料归档/04_STL实验判断/outputs/phase63_delta_mag_summary.md
```

## 本阶段任务

完成 **A5：脚本与输出规范**。

A5 不是继续泛泛规划，而是把 A4 的三维搜索方案落成可执行规范，为 A6 的实际复核做准备。

请重点回答：

1. 小项目内脚本/运行结果/审阅结果应如何组织？
2. A4 的 S1-S5 分级搜索，每一步输入、输出、字段、命名规则是什么？
3. 如何只读调用原大项目代码、STL 或旧结果，同时保证不向小项目外写入？
4. 如何追踪 `face-center (legacy)` 与后续可能的 `pixel-level` 口径？
5. 如何记录总 OCS、per-part OCS、部件占比、遮挡率、Delta m、roll 敏感度、机制标签？
6. `is_near_halfvector` 在缺少法向数据时如何记为 null/unknown？
7. A6 旧五几何预实验复核的最小实施单元是什么？

## 必须遵守的路径规则

严禁删除、修改、移动、覆盖小项目文件夹之外的任何文件。

严禁向小项目文件夹之外添加任何新文件。

可以只读原大项目文件作为证据。

不得把已归档的旧目录：

```text
90_旧结构与支撑材料归档/04_STL实验判断/
```

重新当作活跃输出目录。

Claude 草稿输出到：

```text
06_Claude执行方案/Claude输出/
```

Codex 审阅输出到：

```text
06_Claude执行方案/Codex审阅/
```

通过审阅后的正式成果进入：

```text
00_A0-A10正式成果区/A5_脚本与输出规范/
```

如 A5 需要规定未来实际运行结果路径，请优先采用：

```text
06_Claude执行方案/Claude输出/A6_旧五几何预实验复核_运行草稿/
06_Claude执行方案/Claude输出/A7_新几何与roll实施_运行草稿/
```

通过审阅后再并入对应正式成果区，不要创建新的旧式 `01-05` 或 `04_STL实验判断` 活跃目录。

## A5建议输出结构

请输出一个主文档：

```text
06_Claude执行方案/Claude输出/A5_脚本与输出规范.md
```

文档至少包含：

1. 本阶段目标
2. 读取的原项目文件
3. 写入的小项目文件
4. 脚本边界与文件保护规则
5. 推荐脚本结构
6. S1-S5 输入/输出规范
7. CSV字段规范
8. 口径追踪与数据来源字段
9. 未审阅结果和通过结果的路径规范
10. A6 最小实施单元
11. 不能支持的结论
12. 风险与待确认
13. 给 Codex 的审阅问题
14. 下一阶段建议

如字段较多，可以另外输出：

```text
06_Claude执行方案/Claude输出/A5_输出字段规范表.csv
```

## A5必须包含的字段

几何字段：

```text
geometry_id
geometry_label
phase_angle_deg
sun_direction_I_x
sun_direction_I_y
sun_direction_I_z
det_direction_I_x
det_direction_I_y
det_direction_I_z
range_km
```

姿态字段：

```text
yaw_deg
pitch_deg
roll_deg
```

OCS与部件字段：

```text
total_ocs_model
ocs_jinshuzhuti_model
ocs_taiyangnengban_model
ocs_yinshenban_model
pct_jinshuzhuti
pct_taiyangnengban
pct_yinshenban
occlusion_ratio_percent
```

搜索与追踪字段：

```text
search_stage
candidate_source
data_source
source_file
script_name
run_id
```

机制字段：

```text
dominant_component
dominant_component_fraction
roll_sensitivity_ratio
peak_fwhm_yaw_deg
peak_fwhm_pitch_deg
peak_fwhm_roll_deg
is_near_halfvector
mechanism_label
mechanism_notes
```

相对亮度字段：

```text
delta_mag_vs_reference
reference_attitude_id
reference_definition
```

## 特别注意

- A5 不得把旧 phase63 roll=0 结果称为全局最亮。
- A5 不得把 OCS 直接当作绝对视星等。
- A5 不得混合 face-center 与 pixel-level 口径。
- A5 必须为 A6 实际复核服务；不要再只写“建议后续实施”。
- A6 的最小实施单元建议是：只读旧五几何/phase63 结果，生成统一字段的 top-K 表、per-part 贡献表和 Delta m 表，全部标注 `data_source = face-center (legacy)`。
