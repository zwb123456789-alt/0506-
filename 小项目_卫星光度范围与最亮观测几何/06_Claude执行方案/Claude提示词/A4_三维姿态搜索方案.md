# 给 Claude 的提示词：A4 三维姿态搜索方案

请严格执行 A4。A4 只设计 yaw/pitch/roll 三维姿态搜索方案，不运行仿真，不写执行脚本，不输出数值结果。

## 必读文件

请读取：

```text
00_小项目总览.md
00_原大项目文件保护规则.md
06_Claude执行方案/00_Claude执行总览.md
06_Claude执行方案/README.md
00_A0-A10正式成果区/A0_边界冻结与旧结果体检/A0_边界与旧结果体检.md
00_A0-A10正式成果区/A1_OCS定义与采样逻辑审计/A1_OCS与采样逻辑审计.md
00_A0-A10正式成果区/A3_几何搜索空间设计/A3_几何搜索空间设计.md
00_A0-A10正式成果区/A3_几何搜索空间设计/A3_几何搜索空间矩阵_草案.csv
06_Claude执行方案/Codex审阅/A3_Codex审阅.md
01_说明文档区/04_STL材料与模型说明/00_STL模型与现有结果索引.md
01_说明文档区/04_STL材料与模型说明/01_材料体系充分性与边界.md
01_说明文档区/04_STL材料与模型说明/02_三材料参数审查.md
90_旧结构与支撑材料归档/04_STL实验判断/outputs/phase63_topk_brightness.csv
90_旧结构与支撑材料归档/04_STL实验判断/outputs/phase63_component_contribution.csv
90_旧结构与支撑材料归档/04_STL实验判断/outputs/phase63_delta_mag_summary.md
```

## 保护规则

- 不删除、修改、移动、重命名、覆盖小项目文件夹之外的任何原大项目文件。
- 不向小项目文件夹之外添加任何新文件。
- 不运行仿真。
- 不生成执行脚本。
- 不向 `90_旧结构与支撑材料归档/` 或 `01_说明文档区/` 写入新成果。

## 输出位置

请将 A4 草稿写入：

```text
06_Claude执行方案/Claude输出/A4_三维姿态搜索方案.md
```

## 本阶段目标

完成 A4：三维姿态搜索方案。

请重点回答：

1. yaw/pitch/roll 三维姿态空间如何搜索？
2. 如何避免 yaw=0/360 重复？
3. 如何处理 yaw/pitch 欧拉网格不均匀的问题？
4. roll 扫描如何设计，才能不漏掉太阳能板或金属面 glint？
5. 如何从多来源 top-K 候选进入 roll 扫描和局部细化？
6. 如何控制 A3 几何集合 × 姿态集合的组合爆炸？
7. A4 对 A5 脚本与输出规范提出哪些字段需求？

## 必须继承的边界

- 旧 phase63 结果：yaw=300, pitch=15, roll=0, OCS=14.823573，只是固定 phase63、roll=0、当前材料参数和遮挡语义下的预实验。
- A3 G1 几何集合包含 12 个相位角，其中 phase63 是关键对照点。
- `range_km` 只用于 A9 星等后处理，不进入 OCS 积分。
- 旧五几何是 face-center 旧口径，不与后续 pixel-level 结果混表。

## 推荐策略

请采用分级方案，而不是全空间笛卡尔积：

1. **阶段 A4-S1：旧结果候选提取**
   - 从 phase63 和旧五几何预实验中提取 total OCS top-K、per-part top-K、太阳能板占比 top-K。
2. **阶段 A4-S2：代表几何粗扫**
   - 选择少量代表几何做 yaw/pitch 粗扫，不重复 yaw=360。
3. **阶段 A4-S3：候选姿态 roll 扫描**
   - 对多来源候选姿态扫描 roll，步长给候选方案，不在 A4 强行唯一固定。
4. **阶段 A4-S4：局部三维细化**
   - 对 roll 扫描后 top-K 候选，在 yaw/pitch/roll 周围局部加密。
5. **阶段 A4-S5：机制标注**
   - 为每个候选记录主导部件、贡献占比、遮挡比例、是否接近 half-vector 镜面条件。

## 输出格式

请严格使用以下结构：

```text
# A4_三维姿态搜索方案

## 本阶段目标

## 读取的原项目文件

只列路径和用途，不修改。

## 写入或更新的小项目文件

## 姿态变量定义

## 旧roll=0预实验边界

## 三维姿态搜索总体策略

## 多来源top-K候选设计

## roll扫描方案

## 局部细化方案

## 计算预算与组合爆炸控制

## 机制判据与记录字段

## 对A5脚本与输出规范的约束

## 不能支持的结论

## 风险与待确认

## 给 Codex 的审阅问题

## 下一阶段建议
```

## 特别禁止

- 不要运行仿真。
- 不要写脚本。
- 不要把旧 phase63 roll=0 结果称为真实全局最亮。
- 不要对所有 A3 几何 × 全 yaw/pitch/roll 做无约束全扫描方案。
- 不要固定唯一 roll 步长为最终方案；可以给候选步长与预算比较。
