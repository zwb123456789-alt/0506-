# 下一步计划：A4 启动

生成时间：2026-06-09

## 当前状态

A3 已通过 Codex 审阅，并完成正式并入：

```text
00_A0-A10正式成果区/A3_几何搜索空间设计/A3_几何搜索空间设计.md
00_A0-A10正式成果区/A3_几何搜索空间设计/A3_几何搜索空间矩阵_草案.csv
```

A3 审阅记录保留在：

```text
06_Claude执行方案/Codex审阅/A3_Codex审阅.md
```

## 当前下一步

执行 **A4：三维姿态搜索方案**。

A4 是方案设计阶段，不运行仿真，不写执行脚本，不生成数值结果。

## A4 目标

1. 设计 yaw/pitch/roll 三自由度姿态搜索策略。
2. 继承 A1 的采样逻辑约束：
   - yaw=0 与 yaw=360 不重复；
   - yaw/pitch 欧拉网格不是均匀 SO(3) 采样；
   - roll=0 旧结果只能作为二维切片预实验。
3. 继承 A3 的几何约束：
   - 使用 A3 G1 几何集合；
   - 不对所有几何 × 所有姿态做无约束笛卡尔积全扫描；
   - `range_km` 不进入 OCS 积分。
4. 设计多来源 top-K 候选：
   - total OCS top-K；
   - 金属主体 OCS top-K；
   - 太阳能板 OCS top-K；
   - 太阳能板贡献占比 top-K。
5. 设计粗扫、roll 扫描、局部细化和计算预算控制。

## Claude 输出位置

```text
06_Claude执行方案/Claude输出/A4_三维姿态搜索方案.md
```

通过 Codex 审阅后，再移入：

```text
00_A0-A10正式成果区/A4_三维姿态搜索方案/
```

## A4 通过标准

1. 明确 yaw/pitch/roll 采样策略和边界。
2. 不提前固化不必要的计算参数；具体步长可给候选方案和预算估算。
3. 避免漏掉太阳能板或金属面 glint。
4. 明确旧 phase63 roll=0 只是预实验，不是全局最亮。
5. 为 A5 脚本与输出规范提供清晰输入需求。
