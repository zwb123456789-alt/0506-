# R127 Codex 审阅：109 通过，闭口后增强实验清账完成并放行三轴小项目准备

最后更新：2026-07-01  
审阅端：Codex  
审阅对象：

```text
02_Claude输出/109_1C闭口后增强实验清账_multiSeed_PINTHard_MrollFull_ConformalAlpha_Claude执行报告.md
v0.4_results/17_route1c_postclosure_enhancement_sweep/
```

上游阶段门：R125 已裁定路线一 C 实验主干在当前 model-known simulated / fixed-roll / L1 多几何范围内闭口；R126 下达闭口后增强实验清账任务。

## 1. 审阅结论

109 执行报告与 17 号增强证据包通过。R126 要求的四类增强项均已完成并达到强接收标准：

```text
multi-seed sanity：通过，3/3 seed 保持 L1-G1 -> L1-G3 -> L1-G5 完整单调增益。
P-INT-hard / degraded-severe：通过，clean hard 子集与 severe 9/9 run 均完成；joint 强互补性仍不支持。
M-roll full-2664：通过，phase63 × roll={-30,-15,+15,+30} 全 2664 姿态渲染、后处理与评估完成。
conformal alpha sensitivity：通过，alpha=0.05/0.10/0.20 三档覆盖与 set_size 敏感性完成。
```

R127 裁定：**路线一 C 闭口后增强项清账完成；R125 闭口结论无需修正；可以正式进入三轴小项目准备阶段。**

本裁定不等于三轴小项目已经完成，不启动 T3/L2 光变正式训练，不启动路线二/三/四扩展，不写论文正文，不把本项目写成真实未知目标姿态反演系统。

## 2. 完成度与路径核验

17 号包结构完整：

```text
audit/
conformal_alpha/
figures/
logs/
mroll_full2664/
multiseed/
pint_hard_degraded_severe/
scripts/
synthesis/
tables/
text/
```

Codex 抽查结果：

```text
generated_files_manifest.csv：260/260 OK
numeric_consistency_check.csv：11/11 PASS
redline_self_check.csv：12/12 PASS
figures：6 PNG + 6 PDF 存在
multiseed：6 个新 run
degraded-severe：9 个 run
mroll_full2664：23 个文件
tables：12 个文件
scripts：11 个文件
```

报告写入 `02_Claude输出/`，未生成 Claude 名义的 Codex 审阅裁决文件，未写成果区，未改 `CLAUDE.md`，未启动三轴小项目；`text/codex_review_checklist_for_109.md` 属于 R126 要求的交付清单，不是越权裁决文件。

## 3. 接收证据

### 3.1 Multi-seed sanity

P-INT clean / ocs_only / split seed 固定 42 下，seed=42/7/123 均保持几何数单调增益：

| seed | cMAE G1/G3/G5 | hit@30 G1/G3/G5 |
|---|---:|---:|
| 42 baseline | 76.56 / 38.22 / 22.77 | 0.277 / 0.672 / 0.811 |
| 7 | 80.76 / 39.88 / 19.01 | 0.294 / 0.639 / 0.834 |
| 123 | 76.76 / 38.20 / 20.10 | 0.274 / 0.659 / 0.834 |

接收为：L1 多几何 OCS 主结论对训练随机种子不敏感。seed42 复现 R125/R115 主数字。

### 3.2 P-INT-hard 与 degraded-severe

hard subset 重算显示 clean 条件下 image_only 仍在多数子集接近天花板，joint 与 image_only 基本持平。degraded-severe 全矩阵完成：

| geom | ocs_only hit@30 | image_only hit@30 | joint hit@30 | joint 增量 |
|---|---:|---:|---:|---:|
| G1 | 0.1791 | 0.9966 | 1.0000 | +0.0034 |
| G3 | 0.4730 | 1.0000 | 1.0000 | +0.0000 |
| G5 | 0.5068 | 0.9966 | 1.0000 | +0.0034 |

接收为诚实负向增强：即使 blur sigma=2、downsample x4、flux error 12% 等 severe 退化下，image_only 仍近饱和，joint 增量不足以支持强互补性。附带接收 severe 下 OCS-only 仍保持 G1 -> G3 -> G5 单调改善。

### 3.3 M-roll full-2664

phase63 代表几何下，roll={-30,-15,+15,+30} 均补齐 2664 姿态并完成 distribution-shift 评估。核心结果：

```text
小 roll 扰动：±15° hit@30 约 0.83-0.97，fixed-roll 结论未被直接推翻。
大 roll 扰动：±30° hit@30 降至约 0.53-0.67，fixed-roll 边界明显敏感。
```

R127 接收其为路线一 C 的 roll sensitivity 增强探针。报告中关于 G3/G5 多几何 OCS roll 版“不适用”的说明合格：R126 预注册只渲 phase63 代表几何，因此 ocs_only/joint 多几何 roll 版未评估不是遗漏。不得把本结果写成三轴小项目完成或 roll-aware 训练完成。

### 3.4 Conformal alpha sensitivity

alpha=0.05/0.10/0.20 三档完成。G5 set_size 随 alpha 增大收窄：

```text
ocs_only  : 207.6 -> 126.2 -> 56.2 deg
image_only: 47.7  -> 32.3  -> 24.8 deg
joint     : 14.5  -> 12.9  -> 10.3 deg
```

接收为 SI 增强：只能写 split-conformal 工程覆盖与 set_size 敏感性，不写 Bayesian posterior 或最终概率校准。

## 4. 对 R125 的影响

R127 不修正 R125 闭口裁决，而是增强其稳健性：

```text
增强：OCS 多几何单调增益经 multi-seed 与 severe 退化复核仍成立。
增强：fixed-roll 边界由 312 子集探针扩展为 full-2664 roll sensitivity。
增强：conformal alpha 敏感性可进入 SI 准备材料。
不变：P-EXT yaw-block 坍缩仍不能写成已解决。
不变：joint 强互补性仍未被证明。
不变：image_only 欠覆盖、neural margin 弱等置信边界继续保留。
```

## 5. 裁决问题回答

Q1 multi-seed 是否支持主结论：支持。  
Q2 P-INT-hard/degraded-severe 是否支持 joint 互补性：不支持，维持负向观察。  
Q3 M-roll full-2664 是否完成 fixed-roll 边界增强：完成。  
Q4 conformal alpha 是否可接收为 SI 增强：可接收。  
Q5 R125 闭口结论是否需修正：无需修正，增强不变。  
Q6 是否可正式进入三轴小项目阶段：可以进入三轴小项目准备阶段门设计。

## 6. 成果区升级

同意新增当前主用成果摘要：

```text
01_成果区/00_当前主用成果/11_路线一C闭口后增强实验清账_R127通过.md
```

17 号包本体仍保留在：

```text
v0.4_results/17_route1c_postclosure_enhancement_sweep/
```

## 7. 后续边界

下一步可围绕三轴小项目生成准备任务单，目标应限定为：

```text
最亮构型；
高信息姿态；
低信息/易混淆区域；
观测规划；
路线一 C D4 地图与 M-roll 边界如何作为三轴搜索接口。
```

三轴小项目不得写成真实未知目标三轴姿态反演系统。后续如需正式启动三轴渲染、三轴网格、三轴评价指标或观测规划图表，仍应由 Codex 单独下达阶段门任务。
