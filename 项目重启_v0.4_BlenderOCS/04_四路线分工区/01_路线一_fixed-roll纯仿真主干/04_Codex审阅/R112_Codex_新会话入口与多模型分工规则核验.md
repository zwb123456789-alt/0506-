# R112 Codex 核验：新会话入口与多模型分工规则

最后更新：2026-06-30  
性质：入口文件核验与同步记录  
状态：已修补入口链；不作路线阶段新裁决。

## 1. 本轮检查对象

检查范围：

```text
AGENTS.md
项目重启_v0.4_BlenderOCS/AGENTS.md
项目重启_v0.4_BlenderOCS/CLAUDE.md
04_四路线分工区/01_路线一_fixed-roll纯仿真主干/04_Codex审阅/R110...
04_四路线分工区/01_路线一_fixed-roll纯仿真主干/04_Codex审阅/R111...
```

检查目标：

```text
1. 新会话能否获得当前路线一 C 状态。
2. 新会话能否获得 Claude/Codex 职能边界。
3. 新会话能否获得回复要求、长程任务要求和审阅落盘要求。
4. 新会话能否获得文件夹管理与成果区/审阅区分流规则。
5. 新会话能否看到 R110/R111 对现有结果问答和路线一复位的记忆。
6. 新会话能否从单一权威文件获得“不要再让 Claude 做碎片化叙事小任务，应规划长程执行任务”的效率规则。
7. 新会话能否从单一权威文件获得 Claude 与 Codex 输出篇幅限制。
```

## 2. 发现的问题

### 2.1 v0.4 缺少 AGENTS.md

根目录 `AGENTS.md` 要求新对话进入：

```text
项目重启_v0.4_BlenderOCS/AGENTS.md
```

但检查时该文件不存在。实际存在的是：

```text
项目重启_v0.4_BlenderOCS/CLAUDE.md
```

这会导致新 Codex 会话按根入口继续读取时断在第二步。

### 2.2 CLAUDE.md 内部状态前后冲突

`CLAUDE.md` 顶部已经更新到：

```text
R104/R106 后，头B 进入 B6 长程任务。
```

但第 7 节仍停留在：

```text
R96/P0-FIX01 当前下一步。
```

这会让新会话误判当前仍在 P0 补齐阶段。

### 2.3 根目录 AGENTS.md 仍停在 R82/E45B

根入口仍写：

```text
当前路线一 C 已推进到 R82/E45B 后作者决策点。
当前下一步原则上先做 E45C 图表/SI 规划稿。
```

这与当前 R104/R108/R109/R110/R111 后状态不一致。

### 2.4 R110/R111 未被入口规则强制读取

R110/R111 已保留作者关于当前结果、4维 OCS、yaw-block、1C、小项目和光变/时序方向的关键纠偏，但原入口文件尚未把它们提升为涉及相关问题时的必读记忆。

### 2.5 v0.4 AGENTS.md 初版对效率与篇幅限制写得不够硬

`CLAUDE.md` 已有长程任务、反碎片化和篇幅限制，但 v0.4 `AGENTS.md` 初版只概括了长程任务原则，没有把这些要求显式提升到新会话入口。为避免多文件重复，已将完整规则集中到：

```text
项目重启_v0.4_BlenderOCS/AGENTS.md
第 3 节：Claude 与 Codex 职能
```

## 3. 已执行修补

### 3.1 新增 v0.4 AGENTS.md

新增：

```text
项目重启_v0.4_BlenderOCS/AGENTS.md
```

写入内容包括：

```text
新会话必读包；
当前最高优先级记忆；
R104/R105/R106/R108/R109/R110/R111 必读条件；
Claude 与 Codex 职能边界；
长程任务规则；
反碎片化叙事任务规则；
Claude/Codex 输出篇幅限制；
避免无用复核规则；
文件管理规则；
当前红线；
当前有效下一步：等待 R109/B6-FIX01 结果并由 Codex 审阅。
```

### 3.2 更新 CLAUDE.md

已更新：

```text
项目重启_v0.4_BlenderOCS/CLAUDE.md
```

修改点：

```text
1. 在 R106 后高优先级记忆中加入 R108/R109/R110/R111。
2. 增加不可遗忘结论：C2 4维 per-part OCS 是 semi-oracle/diagnostic，24 号主线 OCS 是 L1，多几何总光度向量。
3. 明确 yaw-block 是 strict extrapolation stress test，不应独占路线一 C 主线。
4. 将第 7 节从 R96/P0-FIX01 替换为当前 R109/B6-FIX01 等待 Claude 返回后审阅。
5. 删除/替换第 6 节中过期的 R96 当前状态红线。
```

### 3.3 更新根目录 AGENTS.md

已更新：

```text
AGENTS.md
```

修改点：

```text
1. 将当前阶段从 R82/E45B 后作者决策点更新为头B B6-FIX01。
2. 将新会话确认项更新为 B6-FIX01 等待 Claude 结果与 Codex 审阅阶段。
3. 将当前 v0.4 最新状态补入 R104/R108/R109/R110/R111。
4. 将当前下一步改为等待 Claude 按 R109 返回 B6-FIX01/102 或后续报告。
5. 将根目录红线更新为不得把 B6 当路线一 C 整体闭口、不得触发头A/头B大合并裁决、不得启动 T3/T4/三轴小项目/路线二三四扩展。
```

## 4. 当前新会话应获得的信息

新会话从大根目录启动时，读取链条应为：

```text
AGENTS.md
项目重启_v0.4_BlenderOCS/AGENTS.md
项目重启_v0.4_BlenderOCS/CLAUDE.md
```

新会话应得到：

```text
1. 当前项目焦点是 v0.4 BlenderOCS。
2. 当前路线一 C 在头B B6-FIX01，等待 Claude 多折补齐结果。
3. Codex 是审阅/裁决/调度端，Claude 是执行端。
4. Claude 输出进 02_Claude输出，Codex 审阅和返工单进 04_Codex审阅。
5. Codex 审阅不通过必须生成 Markdown 审阅/FIX 文件，不能只口头驳回。
6. 后续 Claude 任务应为长程执行任务，避免碎片化叙事任务；完整规则只在 v0.4 `AGENTS.md` 保留。
7. Claude 报告和 Codex 审阅必须受篇幅限制约束；完整规则只在 v0.4 `AGENTS.md` 保留。
8. R110/R111 是现有结果问答和路线一复位的必读记忆。
9. B6-FIX01 不能闭整个路线一 C，也不能触发头A/头B合并裁决。
```

## 5. 仍需注意

检查时发现 v0.4 根目录内存在一个同名嵌套目录：

```text
项目重启_v0.4_BlenderOCS/项目重启_v0.4_BlenderOCS/
```

该目录下含旧的 `04_四路线分工区/` 与 `v0.4_results/` 片段，疑似早期误嵌套副本。本轮未删除、未移动、未清理。后续若涉及路径混淆，可另行做只读核验后再决定是否归档。

## 6. 结论

本轮已完成新会话入口修补。新对话应能接收到：

```text
Claude/Codex 职能边界；
回复和审阅落盘要求；
文件夹分流规则；
长程任务优先、避免碎片化叙事任务互审的效率规则；
Claude/Codex 输出篇幅限制；
以上两类规则以 v0.4 `AGENTS.md` 为唯一展开文件，其他文件只作指向；
R110/R111 记忆要求；
当前 B6-FIX01 状态；
不得提前合并裁决或闭路线一 C 的限制。
```

本文件不作新的科学裁决，不放行新训练、成果区写入、论文正文改写、T3/T4、三轴小项目或路线二/三/四扩展。
