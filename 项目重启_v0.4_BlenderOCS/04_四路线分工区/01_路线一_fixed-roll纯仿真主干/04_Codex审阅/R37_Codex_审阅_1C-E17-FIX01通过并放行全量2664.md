# R37 Codex 审阅：1C-E17-FIX01 full-run driver 启动保护

最后更新：2026-06-24  
审阅端：Codex  
被审阅报告：`02_Claude输出/38_1C-E17-FIX01_fullrun_driver启动保护_Claude执行报告.md`

---

## 0. 裁决

```text
1C-E17-FIX01：PASS
full-run driver 启动保护：PASS
4 姿态小规模验证：PASS
全量 2664 姿态生成：RELEASED FOR ROUTE 1C / Phase 0 B0 full-run
训练：NOT RELEASED
论文正文改写：NOT RELEASED
下一步：1C-E18，执行受控全量 2664 生成并回交完整报告
```

结论：R36 的三个阻断点已经闭合。`render_full_2664_shadow.py` 已具备默认 skip-existing、`--force`、可审计分批参数和每姿态状态 metadata；`run_phase1_fullrun.py` wrapper 已补齐入口兼容；4 姿态验证链路经过 Codex 复跑 checker，17/17 PASS。因此，本轮正式放行路线一 C Phase 0 的 B0 full-run 2664 姿态数据生成。

本放行只覆盖：

```text
Blender shadow passes -> postprocess -> manifest -> checker
```

不覆盖训练、论文正文改写、路线扩展或三轴小项目启动。

---

## 1. Codex 核验证据

### 1.1 报告与文件在位

Codex 已确认：

```text
02_Claude输出/38_1C-E17-FIX01_fullrun_driver启动保护_Claude执行报告.md exists
06_v0.4_code/02_blender/render_full_2664_shadow.py exists
06_v0.4_code/05_postprocess/run_full_postprocess.py exists
06_v0.4_code/05_postprocess/run_phase1_fullrun.py exists
```

`run_phase1_fullrun.py` 是兼容 wrapper，实际转发到 `run_full_postprocess.py` 的 `main()`。后续正式入口以 `run_full_postprocess.py` 为主；wrapper 仅用于兼容 R35 规划命名。

### 1.2 Blender driver 实现核查

`render_full_2664_shadow.py` 已实现：

```text
--smoke
--labels
--start-index
--count
--batch-index
--batch-size
--force
--no-skip-existing
```

姿态选择优先级：

```text
labels > smoke > start-index/count > batch-index/batch-size > full
```

默认：

```text
skip_existing = True
force = False
```

预扫描逻辑已经区分：

```text
COMPLETE -> SKIPPED_EXISTING
PARTIAL  -> PARTIAL_EXISTING，除非 --force
MISSING  -> RENDERED 队列
FAILED   -> FAILED
```

metadata 已记录：

```text
selection_mode
selection_detail
skip_existing
force
selected_count
rendered_count
skipped_count
partial_count
failed_count
results[].status
```

这满足 R36 P0-1 和 P0-2 的放行要求。

### 1.3 FIX01 metadata 结果

文件：`v0.4_results/01_fullrun/shadow_passes/render_metadata.json`

Codex 读取到：

```text
total_grid_size = 2664
selection_mode  = labels
selected_count  = 4
rendered_count  = 1
skipped_count   = 3
partial_count   = 0
failed_count    = 0
skip_existing   = True
force           = False
```

逐姿态状态：

```text
yaw010_pitch+000_roll+000  SKIPPED_EXISTING  camera=True  sun=True
yaw020_pitch+000_roll+000  SKIPPED_EXISTING  camera=True  sun=True
yaw025_pitch+015_roll+000  SKIPPED_EXISTING  camera=True  sun=True
yaw030_pitch+000_roll+000  RENDERED          camera=True  sun=True
```

这证明启动保护不是只写在代码里，而是已经通过真实小规模运行验证。

### 1.4 Postprocess summary

文件：`v0.4_results/01_fullrun/postprocess/fullrun_postprocess_summary.json`

Codex 读取到：

```text
overall_status = COMPLETE
n_total_labels = 4
n_completed    = 4
brdf_branch    = B0
geom_id        = phase63
```

4 条记录均为 `COMPLETE`，并且均有 `sun_visibility_mask_path`。

### 1.5 Checker 复跑

Codex 复跑命令：

```powershell
"C:\Users\97466\.conda\envs\ocs_sim\python.exe" `
  06_v0.4_code\06_manifest\check_manifest_consistency_v0_4.py `
  --ocs-manifest v0.4_results/01_fullrun/postprocess/ocs_manifest_v0_4_fix01.json `
  --image-manifest v0.4_results/01_fullrun/postprocess/image_manifest_v0_4_fix01.json `
  --step6-summary v0.4_results/00_validation/phase0_step6_small_trial/phase0_step6_small_trial_summary.json `
  --data-root . `
  --require-prefix v0.4_results/ `
  --expected-record-count 4 `
  --output v0.4_results/01_fullrun/postprocess/consistency_check_report_fix01_codex_rerun.json
```

结果：

```text
overall_status = PASS
check_count    = 17
records_completeness = PASS, OCS=4, Image=4, expected=4
path_base_consistency = PASS, 0 inconsistencies
ocs_paths_exist = PASS, 0 missing
image_paths_exist = PASS, 0 missing
camera_matrix_non_null_and_valid = PASS, 0 invalid
sun_visibility_mask_path_non_null_and_exists = PASS, 0 missing
```

因此，FIX01 的小规模验证成立。

---

## 2. R36 问题闭合判定

| R36 问题 | Codex 判定 | 依据 |
|---|---|---|
| P0-1：缺少 skip-existing / resume | PASS | 默认 skip-existing、`--force`、`PARTIAL_EXISTING` 保护已实现；3/3 既有姿态 skip 实测通过 |
| P0-2：缺少可审计分批入口 | PASS | `--start-index/--count` 与 `--batch-index/--batch-size` 已实现，metadata 记录 selection detail |
| P1-1：后处理入口命名不统一 | PASS | `run_phase1_fullrun.py` wrapper 已创建，正式入口统一说明为 `run_full_postprocess.py` |

---

## 3. 全量放行范围

本轮放行：

```text
路线一 C / Phase 0 / B0 engineering baseline
全量 2664 fixed-roll 姿态：
1. Blender camera/sun shadow pass EXR
2. BRDF/OCS/image postprocess
3. OCS/Image manifest
4. checker 17 项一致性门禁
```

仍不放行：

```text
1. 模型训练
2. 论文正文改写
3. B1 正式材料公式替换
4. GGX 对照批量生成
5. 三轴小项目
6. 路线二/路线三/路线四扩展
```

---

## 4. 1C-E18 执行要求

Claude 下一步执行 `1C-E18`：受控全量 2664 生成。

### 4.1 Stage A：Blender shadow pass 分批

建议按 200 姿态一批执行，共 14 批：

```powershell
cd "D:\我的文件\研究生学术\光学项目\0506新\项目重启_v0.4_BlenderOCS"

"D:\Program Files\Blender Foundation\Blender 4.2\blender.exe" --background `
  --python "06_v0.4_code\02_blender\render_full_2664_shadow.py" `
  -- --start-index 0 --count 200

"D:\Program Files\Blender Foundation\Blender 4.2\blender.exe" --background `
  --python "06_v0.4_code\02_blender\render_full_2664_shadow.py" `
  -- --start-index 200 --count 200
```

后续批次类推：

```text
0, 200, 400, ..., 2400, 2600
最后一批：--start-index 2600 --count 64
```

要求：

- 默认使用 skip-existing；
- 不使用 `--force`，除非针对明确坏文件做局部修复并在报告中说明；
- 每批结束后记录 metadata 中 `rendered_count`、`skipped_count`、`partial_count`、`failed_count`；
- 若任一批出现 `PARTIAL_EXISTING` 或 `FAILED`，暂停全量流程，先修复该批，不继续推进 postprocess 全量。

### 4.2 Stage B：Postprocess 全量

Blender 2664 camera/sun pair 完整后，执行：

```powershell
"C:\Users\97466\.conda\envs\ocs_sim\python.exe" `
  "06_v0.4_code\05_postprocess\run_full_postprocess.py" --all
```

若中断，使用：

```powershell
"C:\Users\97466\.conda\envs\ocs_sim\python.exe" `
  "06_v0.4_code\05_postprocess\run_full_postprocess.py" `
  --all `
  --resume "v0.4_results\01_fullrun\postprocess\fullrun_postprocess_summary.json"
```

要求：

- 最终 `fullrun_postprocess_summary.json` 必须 `overall_status=COMPLETE`；
- `n_total_labels=2664`；
- `n_completed=2664`；
- 所有 records 均有 `sun_visibility_mask_path`。

### 4.3 Stage C：Manifest 与 checker

全量后处理完成后，构建正式 full-run manifest。建议文件名不要覆盖 smoke/fix01：

```text
v0.4_results/01_fullrun/postprocess/ocs_manifest_v0_4_fullrun.json
v0.4_results/01_fullrun/postprocess/image_manifest_v0_4_fullrun.json
v0.4_results/01_fullrun/postprocess/consistency_check_report_fullrun.json
```

checker 必须使用：

```text
--data-root .
--require-prefix v0.4_results/
--expected-record-count 2664
```

最终要求：

```text
overall_status = PASS
check_count = 17
records_completeness = PASS, expected=2664
ocs_paths_exist = PASS
image_paths_exist = PASS
camera_matrix_non_null_and_valid = PASS
sun_visibility_mask_path_non_null_and_exists = PASS
```

---

## 5. 回交报告要求

Claude 完成 1C-E18 后，写入：

```text
02_Claude输出/39_1C-E18_全量2664生成与manifest_checker_Claude执行报告.md
```

报告必须包含：

1. 全量是否启动、是否完成、是否有中断；
2. 14 批 Blender shadow pass 的每批起止索引、姿态数、`RENDERED/SKIPPED_EXISTING/PARTIAL_EXISTING/FAILED` 统计；
3. 最终 camera EXR 数、sun EXR 数；
4. postprocess summary 的 `overall_status/n_total_labels/n_completed`；
5. manifest 文件路径；
6. checker 17 项结果；
7. 若有失败或局部重跑，列出具体 label、原因、修复方式；
8. 明确声明未训练、未改论文正文、未触碰冻结文件 13/14/24/25、未写 `04_Codex审阅/`。

---

## 6. 红线

1C-E18 放行后仍禁止：

- 启动任何训练；
- 修改论文正文；
- 改写冻结文件 13/14/24/25；
- 扩展到 B1/GGX/三轴/路线二/路线三/路线四；
- 由 Claude 写入 `04_Codex审阅/`；
- 由 Claude 自行宣布“最终验收”或“训练放行”。

若 2664 full-run checker 未达到 17/17 PASS，必须停在 1C-E18 返工，不得进入训练或论文阶段。

---

## 7. 给 Claude 的下一步指令摘要

```text
执行 1C-E18：受控全量 2664 生成。

允许：
- 按 R37 Stage A 分批运行 render_full_2664_shadow.py；
- 完成 2664 camera/sun EXR；
- 运行 run_full_postprocess.py --all；
- 构建 fullrun OCS/Image manifest；
- 运行 checker，--expected-record-count 2664。

必须：
- 使用 skip-existing 默认保护；
- 分批记录状态；
- 出现 PARTIAL_EXISTING/FAILED 时暂停并先修复；
- 最终 checker 17/17 PASS；
- 报告写入 02_Claude输出/39_1C-E18_全量2664生成与manifest_checker_Claude执行报告.md。

禁止：
- 训练；
- 改论文正文；
- 改冻结文件 13/14/24/25；
- 写入 04_Codex审阅/；
- 自行放行下一阶段。
```

---

## 8. 备注：CLAUDE.md 状态

Codex 审阅时发现根目录 `CLAUDE.md` 的“当前状态/下一步”仍停留在 1C-E14 前后，已明显滞后于 R37。由于该文件自身写有“非审阅文件修改需先确认”的规则，本轮未直接修改 `CLAUDE.md`。建议在 1C-E18 启动前由作者确认是否允许同步 `CLAUDE.md` 当前状态，至少更新为：

```text
R37 已放行路线一 C Phase 0 B0 full-run 2664 生成；
训练、论文正文改写、B1/GGX/三轴扩展仍未放行。
```

