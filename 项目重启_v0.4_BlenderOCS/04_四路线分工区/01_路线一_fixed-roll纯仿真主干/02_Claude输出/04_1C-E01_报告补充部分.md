### D. ocs_core.py（历史禁用）

**判定结果**：❌ **历史禁用，不并入路线一 C 主链**

**禁用原因**：
1. **Face-center 采样**：旧模块 A 的 face-level OCS 计算，已被 v0.4 pixel-level 方法取代
2. **与统一前向模型不兼容**：不基于 Blender geometry pass，无法与图像通道同源

**关键函数**（仅作参考，不迁移）：
- `compute_single_attitude()` - 单姿态 OCS 计算，使用面元中心采样
- `scan_attitude()` - 姿态扫描，支持串行/并行
- `_worker_compute()` - 多进程 worker

**可参考但不可迁移的内容**：
- BRDF 调用模式：`eval_brdf(normals, sun_norm, det_norm, mat)`
- 遮挡检测结构：`ray_forest.batch_occlusion_dual()`
- 姿态网格生成逻辑

**依赖库**：
- `numpy`
- `trimesh` - 光线追踪、BVH 遮挡检测
- `tqdm` - 进度条
- `multiprocessing` - 并行计算
- 自定义模块：`config`, `materials`, `geometry`, `occlusion`, `brdf_models`

---

### E. run_multi_geom.py

**角色判定**：可复用参考（需大幅改造）

**当前功能**：
- 多观测几何批量扫描（5 组 sun/det 方向）
- 对每个几何独立执行完整姿态扫描
- 输出 OCS CSV/JSON + 可视化图表

**关键逻辑**（可参考部分）：
1. 多几何循环：`for sun_vec, det_vec, geom_label in OBS_GEOMETRIES`
2. 相位角计算：`_compute_phase_angle(sun_norm, det_norm)`
3. 输出结构：每个几何一个子目录，包含 `ocs_scan.csv` + `ocs_scan.json`
4. 全局汇总：`multi_geom_manifest.json`

**需要大幅改造的部分**：
1. **主链切换**：从调用 `ocs_core.scan_attitude()`（face-center）改为调用 Blender 渲染 + Python BRDF 后处理流程
2. **输出目录**：从 `结果/模块A_重构/multi_geom_ggx_yaw73_pitch37/` 改为符合 14 号规范的路径
3. **Manifest 格式**：从旧 JSON 格式升级到 14 号规范的 `ocs_manifest_v0.4.json` 格式

**可复用部分**：
- 多几何循环框架
- 相位角计算函数
- 汇总 manifest 结构思路

---

### F. render_geometry_passes.py

**角色判定**：可复用参考（需升级）

**当前已有 pass 表**：

| Pass 名称 | 通道 | 数据类型 | 说明 |
|---|---|---|---|
| Combined | RGB | float32 | Blender 渲染输出（本项目不使用） |
| Normal | XYZ | float32 | 世界空间法线 |
| Depth | V | float32 | 深度（depth < 1e9 为物体） |
| IndexOB | V | int32 | 部件 ID：1=jinshuzhuti, 2=taiyangnengban, 3=yinshenban |
| Backfacing | R | float32 | 背面标记：0.0=正面，1.0=背面（AOV） |

**缺失 pass 表（与 13 号规范对比）**：

| 缺失内容 | 13 号规范要求 | 当前状态 |
|---|---|---|
| Position/WorldCoord | ✅ 必需 | ❌ 不存在 |
| Sun-view depth pass | ✅ 必需（Level 2 visibility） | ❌ 不存在 |
| camera_matrix_world 记录 | ✅ 必需 | ❌ 未显式记录到 metadata |
| sun_camera_matrix_world 记录 | ✅ 必需 | ❌ 不存在 |

**与 13 号规范的差距**：
1. **P0 缺失**：无 Position/WorldCoord pass（sun shadow reprojection 必需）
2. **P0 缺失**：无 sun-view depth pass 渲染函数
3. **P1 缺失**：未记录 camera/sun 矩阵到 metadata

**可复用部分**：
- camera-view geometry pass 渲染框架
- MultiLayer EXR 合成器设置
- 正交投影相机设置：`ortho_scale = 2.2 × r_max`
- 姿态应用逻辑

---

### G. brdf_postprocess.py

**角色判定**：可复用参考（需大幅修改）

**当前 BRDF 公式摘录**：

```python
# OCS 积分（第 251-252 行）
radiance[m_front] = f_r * NoL  # 不含 pixel_area

# OCS 总和（第 258 行）
OCS_image = Σ radiance · pixel_area
```

**与 Codex 正式设计的差距**：

| 项目 | 历史快照 | 路线一 C / 13 号规范 | 差距 |
|---|---|---|---|
| 图像线性响应 | `I_linear = f_r · NoL` | `I_linear = f_r · NoL · V_sun_macro` | ❌ **缺少 V_sun_macro** |
| OCS 积分 | `OCS = Σ A_pix · f_r · NoL` | `OCS = Σ A_pix · f_r · NoL · V_sun_macro` | ❌ **缺少 V_sun_macro** |
| 默认 BRDF | GGX/Cook-Torrance | 五参数冯主锚点 | ⚠️ 默认不符 |
| V_sun_macro 实现 | 不存在 | Level 2 sun shadow reprojection | ❌ **完全缺失** |
| I_scale 归一化 | 全局 `radiance_max_global` | corpus-level 两阶段流程 | ⚠️ 需改造 |

**当前可复用部分**：
1. EXR 多层读取：`read_multilayer_exr()` 
2. 像素级 BRDF 计算框架（向量化）
3. Per-part OCS 积分逻辑
4. IndexOB 部件分区

**必须改造部分**：
1. **P0 必改**：加入 `V_sun_macro` 到 OCS 和图像计算
2. **P0 必改**：实现 corpus-level I_scale 两阶段流程
3. **P1 改造**：图像编码从 gamma 2.2 改为 log1p
4. **P1 改造**：支持 Phong-like BRDF 为主线，GGX 为对照

---

### H. 反演代码核对（inv_common.py / train_mlp.py / train_cnn.py / train_fusion.py）

**H1. inv_common.py - 反演公共工具**

**当前能力表**：
- ✅ 读取 `multi_geom_manifest.json` 和多个 `ocs_scan.csv`
- ✅ 特征模式选择：obs_total(5D) / total(15D) / per_part(30D) / all(45D)
- ✅ Split 生成：LOO / coarse-to-fine (10°→5°) / random
- ✅ 预处理：log10 + zscore
- ✅ 误差度量：angular_err_deg, yaw_err, Hit@5°, Hit@10°
- ✅ kNN baseline 框架

**与 14 号规范的差距**：
1. **Manifest schema**：读取旧格式 `ocs_scan.json`，不是 14 号的 `ocs_manifest_v0.4.json`
2. **Source_data.json**：不生成，不记录六子版本
3. **一致性检查**：不检查 OCS + image manifest 的 geometry/BRDF/visibility 版本一致性

**可复用部分**：
- 特征提取框架
- Split 生成逻辑（coarse-to-fine 可直接用）
- 误差度量函数
- zscore/log 变换

---

**H2-H4. 训练脚本（train_mlp.py / train_cnn.py / train_fusion.py）**

**共同输出**：
- ✅ 训练曲线 CSV、测试预测 CSV、Metrics JSON、config_used.json
- ❌ 不生成 source_data.json
- ❌ 不记录六子版本（geometry/BRDF/visibility/ocs/image/dataset）

**与 14 号规范的差距**：
1. 不生成 `source_data.json`（必需字段见 14 号 §5）
2. 不记录 `method_version` 和六子版本
3. Latest-run 自动发现（14 号禁止）

---

## 4. 依赖库清单

### Python 标准库
- `os`, `sys`, `csv`, `json`, `glob`, `argparse`, `time`, `datetime`, `pathlib`, `multiprocessing`

### 第三方库（按模块分类）

**几何与 STL**：
- `trimesh` - STL 加载、网格抽稀、BVH 光线追踪
- `numpy` - 数值计算（所有模块通用）

**Blender 渲染**：
- `bpy` - Blender Python API（仅 Blender 环境）
- `mathutils` - Blender 向量/矩阵（仅 Blender 环境）

**图像与 EXR**：
- `OpenEXR` - EXR 多层读写
- `Imath` - OpenEXR 辅助
- `PIL` (Pillow) - PNG 读写
- `imageio` - 图像 I/O（可选）
- `cv2` (OpenCV) - 图像 I/O（可选）

**深度学习**：
- `torch` - PyTorch 训练框架

**机器学习**：
- `sklearn` (scikit-learn) - kNN baseline

**进度显示**：
- `tqdm` - 进度条

---

## 5. 与 Codex 正式设计的差异表

### 5.1 历史快照有，但路线一 C 不允许直接用的内容

| 内容 | 历史位置 | 不允许原因 |
|---|---|---|
| Face-center OCS 采样 | `ocs_core.py` | 已废弃，v0.4 改为 pixel-level |
| `NUM_YAW = 73` | `config.py` | 包含 360° 重复，应为 72 |
| `BRDF_MODEL = "ggx"` 默认 | `config.py` | 主线应为 Phong-like，GGX 为对照 |
| 旧输出路径 | `config.py:OUTPUT_DIR` | 不符合 14 号规范 |
| Gamma 2.2 PNG 编码 | `brdf_postprocess.py` | 应改为 log1p 编码 |
| 不含 V_sun_macro 的公式 | `brdf_postprocess.py` | 必须加入 V_sun_macro |
| Latest-run 自动发现 | `train_*.py` | 14 号禁止，必须显式传入 |

---

### 5.2 路线一 C 必须有，但历史快照缺失的内容

| 缺失内容 | 路线一 C / 13/14 号要求 | 历史状态 |
|---|---|---|
| Position/WorldCoord pass | 13 号 P0 必需 | ❌ 完全缺失 |
| Sun-view depth pass 渲染 | 13 号 P0 必需（Level 2） | ❌ 完全缺失 |
| V_sun_macro reprojection | 13 号 P0 必需（Level 2） | ❌ 完全缺失 |
| Depth round-trip 校验 | 13 号 P1 gate | ❌ 完全缺失 |
| 20 姿态 shadow validation | 13 号 P0 gate | ❌ 完全缺失 |
| 五参数冯 BRDF 主锚点实现 | 路线一 C 主线 | ⚠️ Legacy Phong 可能等价，待确认 |
| OCS manifest v0.4 | 14 号 P0 | ❌ 完全缺失 |
| Image manifest v0.4 | 14 号 P0 | ❌ 完全缺失 |
| source_data.json | 14 号 P0 | ❌ 完全缺失 |

---

### 5.3 历史快照可改造复用的内容

| 内容 | 改造点 | 改造难度 |
|---|---|---|
| `materials.py` | 确认参数与书中对齐，支持双 BRDF 模式 | 低 |
| `geometry.py` | 直接复用，检查坐标系一致性 | 极低 |
| `config.py` | NUM_YAW 改72, BRDF_MODEL 主线改Phong, 路径更新 | 低 |
| `render_geometry_passes.py` | 新增 Position AOV, sun-view depth, 矩阵记录 | 中 |
| `brdf_postprocess.py` | 加 V_sun_macro, 改I_scale流程, 改log1p编码 | 中高 |
| `run_multi_geom.py` | 主链从face-center改为Blender+Python流程 | 高 |
| `inv_common.py` | Manifest schema 升级, 一致性检查 | 中 |
| `train_*.py` | 新增 source_data.json 生成 | 中 |

---

## 6. 禁止迁移清单

以下旧模块**不并入路线一 C 主链**，仅保留为历史参考：

| 模块 | 路径 | 禁止原因 |
|---|---|---|
| Face-center OCS 采样 | `01_code/ocs_core.py` | 已废弃，v0.4 改为 pixel-level，与统一前向模型不兼容 |
| Face-center 主入口 | `01_code/main_run.py` | 调用旧 ocs_core.py（实际不存在于快照） |
| A/B 一致性检查 | `02_blender/analyze_consistency.py` | v0.4 改为 manifest schema 一致性 |

**理由**：这些模块基于旧的 face-center 采样方法，与 v0.4 的 Blender pixel-level 同源架构根本不兼容。保留为历史参考以理解"为什么 v0.3 封存"，但不能迁移到新代码区。

---

## 7. 待 Codex 审阅问题清单

**只列问题，不给路线设计建议。**

### Q1. BRDF 主线确认
- Legacy Phong 的公式 `f_r = rho_d/π + rho_s·(N·H)^n` 是否等同于书中的"五参数冯 / Phong-like BRDF"？
- 如果等价，是否可以直接复用 materials.py 的 Legacy Phong 参数？
- 如果不等价，五参数冯的完整公式是什么？

### Q2. 材料参数对齐
- materials.py 中的 rho_d/rho_s/n 参数是否已与书籍知识库材料参数对齐？
- 如果未对齐，书中材料参数的具体值是什么？位于哪一页或章节？

### Q3. 代码区与输出区路径
- 是否创建 `06_v0.4_code/` 作为路线一 C 正式代码区？
- 输出根目录采用 `v0.4_results/` 还是 `07_v0.4_results/`？
- 历史快照中可复用模块的迁移策略：复制、符号链接还是完全重写？

### Q4. V_sun_macro 实现顺序
- 是否需要先完成 depth round-trip 验证（3 已知点），再实现完整的 sun shadow reprojection？
- depth_epsilon_m 初始值 1e-3 m 是否合适？

### Q5. Corpus-level I_scale 实现方式
- 两阶段流程：是否需要独立的 dry-run 统计脚本（Pass 1），还是在 BRDF 后处理中增加两阶段逻辑？

### Q6. 20 姿态 shadow validation 的姿态选择
- 具体 20 个 yaw/pitch 组合由谁规划？

---

## 8. 结尾声明

**本报告仅为 Claude 执行核对输出，不包含设计裁决。**

以下事项**等待 Codex 审阅与作者确认**：
1. 是否创建 `06_v0.4_code/` 和 `07_v0.4_results/` 或其他路径
2. 如何迁移历史快照中的可复用代码
3. 五参数冯 BRDF 与 Legacy Phong 的等价性
4. 材料参数是否需要与书籍知识库对齐
5. V_sun_macro 实现的优先级和顺序
6. 是否进入实现阶段

**未经 Codex 审阅和作者确认，不执行任何代码创建、复制、修改或运行操作。**

---

**报告完成时间**：2026-06-22  
**下一步**：等待 Codex 审阅本报告，并给出路线一 C 代码任务执行指令。
