# R159 Codex 审阅：017-020 D 类四合一接收与分流

最后更新：2026-07-06  
审阅对象：`04_四路线分工区/00_总览与裁决/02_Claude输出/017-020`  
任务类型：D 类只读/文档候选材料审阅  
Codex 裁决：分项接收；暂不直接迁正式索引，不直接瘦身 CLAUDE.md  

## 1. 总体裁决

017-020 四个文件作为 D 类只读/文档材料通过首轮 Codex 审阅，均可作为后续写作和流程整理的候选输入。但它们目前仍位于 `02_Claude输出/`，属于 Claude 候选材料，不是正式入口文件、冻结文件或论文正文。

因此本轮分流为：

```text
017 论文骨架表：接收为候选骨架表，写作期可用，但需 Codex/作者二次定稿后才迁正式维护表。
018 术语映射表：接收为候选术语表，可立即作为写作和冷启动外审的术语约束参考。
019 审阅链索引：接收为候选索引草稿，不直接迁为正式索引；迁移与 CLAUDE.md 瘦身需单独受控任务。
020 GEO 数据阻塞说明：接收为正式阻塞判断输入；GEO 两图/N5 真实 glint 检验在无数据本体前不得启动。
```

## 2. 分项审阅

### 017 论文骨架表

017 将主图/主表拆成 10 图 5 表，并标明证据强度与缺口任务。Codex 接收其总体结构判断：

```text
路线一 C 和三轴机制部分证据基本齐备；
投稿档位继续上探的瓶颈主要在真实数据锚定、接管边界、置信选择性与外部基线。
```

修正边界：

```text
Fig.8 需要按 R158 更新为“近镜面 fixed top-1 + 金属宽瓣/几何因子角落高亮”双机制；
Fig.9/Fig.10 仍是数据阻塞，不得写成已有真实验证；
Table 3/Table 5 仍是缺口，不得进入主 claim。
```

### 018 术语映射表

018 对内部名到论文名的映射基本合格，尤其已登记：

```text
metal wide-lobe / geometric-factor highlight
corner-geometry bright candidate
template-retrieval baseline (model-known)
attitude-truth-free GEO photometric survey database
```

Codex 接收为写作期术语约束。后续冷启动外审材料应尽量只使用“论文名”列，不暴露 R 编号、包编号和内部阶段标签。

### 019 审阅链索引

019 自动索引实际文件名，覆盖面有价值，但当前仍有三点限制：

```text
1. 它是 Claude 输出，不是正式事实源。
2. 它生成时最新三轴状态停在 R157 待审，需补 R158/R159/R160 后才能正式化。
3. 迁移为 `00_审阅链索引.md` 会牵涉 CLAUDE.md 瘦身和入口规则，必须单独执行，不能在本轮顺手改。
```

因此接收为“正式索引迁移任务的草稿输入”，暂不迁移。

### 020 GEO 数据阻塞说明

020 的阻塞判断接收：

```text
项目内目前只有 GEO 数据库说明文档，没有 DET/PRE/REG/MAN/FITS 数据本体；
真实相位角覆盖图、仿真 vs 真实光度分布图、N5 glint 检索均无法在当前材料上执行。
```

最小数据需求清单接收：

```text
1. DET/IPD 逐帧测光
2. PRE 几何预报
3. REG 型号身份
```

在上述数据到位前，不得启动真实 GEO 锚点图和真实 glint 验证 claim。可先做字段规范、合成数据脚本骨架、OCS 到视星等换算链路和 N5 预注册草案。

## 3. 后续处理

不立即修改：

```text
CLAUDE.md 瘦身
AGENTS.md 阶段状态转移
00_审阅链索引.md 正式迁移
论文正文
路线二/三/四启动
```

建议后续单独下达一个入口整理任务：

```text
R161：入口瘦身与正式审阅链索引迁移
```

该任务应由 Codex 主导，基于 019 草稿补齐 R158/R159/R160，并将 `CLAUDE.md` 从历史长清单改为状态表 + 索引链接。
