# R05 Codex 当前任务顺序：按 86 号两头并行与合并审阅执行

最后更新：2026-06-29  
审阅端：Codex  
依据文件：

```text
04_四路线分工区/01_路线一_fixed-roll纯仿真主干/02_Claude输出/
86_后续路线结构_两头并行与合并审阅说明_Claude整理.md

04_四路线分工区/00_总览与裁决/04_Codex审阅/
R04_Codex_暂停点复盘_R84后前期后果与后期规划.md
```

性质：当前任务顺序与阶段调度文件；不放行新训练、论文正文正式改写、三轴小项目、路线二/三/四扩展；不替代 E45D-FIX01 正式审阅。

## 0. 文件归属裁决

86 号提出的后续节奏不应写入成果区，也不应立即同步进 `CLAUDE.md`。

当前最合适位置是：

```text
04_四路线分工区/00_总览与裁决/04_Codex审阅/
```

理由：

```text
1. 86 号不是成果本体，而是路线调度与阶段门结构说明。
2. 它同时涉及路线一 R04 收口、文献补课、方法总结、模型改正候选和合并审阅，超出单一 Claude 输出职责。
3. 当前尚未完成 E45D-FIX01 正式审阅，不宜把新顺序直接写入 CLAUDE.md 作为稳定状态。
4. 待后续阶段门通过并形成稳定下一步后，再受控同步 CLAUDE.md。
```

因此，本 R05 作为当前执行顺序的 Codex 侧锚点。

## 1. 当前总任务结构

后续不按简单线性顺序：

```text
文献检索 -> 方法总结 -> 回 R04 阶段门
```

而按 86 号确定为：

```text
头A：R04 主线收口
头B：84/85 后续跟进
最终：两头合并审阅 / 总裁决，重定下一阶段入口
```

核心判断：

```text
R04 不是新工作的起点，而是暂停前路线一 C 负结果/图表主线的收口点。
当前任务是先把 R04 这头应闭合到哪里看清，
同时推进 84/85 这头的认知与方法规划，
最后合并审阅，而不是从任一头单独直接进入新训练或正文。
```

## 2. 头A：R04 主线收口

头A 目标不是完整论文正文，而是形成：

```text
负结果证据链 + 图表/SI 资产 + 负结果到 24 号三问的桥接说明
```

当前头A 最短链：

```text
A-1  Codex 正式审阅 E45D-FIX01
     输入：
       04_四路线分工区/01_路线一_fixed-roll纯仿真主干/02_Claude输出/
       83_1C-E45D-FIX01_FigureS5样本数与版式修正_Claude执行报告.md

A-2  若 A-1 通过，再决定图表/表格资产收尾范围
     优先仅做必要项，不扩大为完整写作阶段。

A-3  写“负结果 -> 24 号三问”桥接材料
     回答：
       what can be known
       when complementary
       when trustworthy
     A-3 才是头A 真正闭合口。
```

头A 红线：

```text
不启动新训练。
不改 split / 模型 / 超参 / seed。
不改论文正文正式段落。
不把 exact-bin 0% 写成物理不可观测。
不把 joint 无增益写成 OCS/image 永久不互补。
不外推真实 GEO、三轴姿态或暗室实验。
```

## 3. 头B：84/85 后续跟进

头B 目标是把后续方法方向想清，而不是立刻启动实验。

当前头B 顺序：

```text
B-1  文献检索
     按 85 号 6 个方向执行：
       光变曲线/光度反演
       SSA 空间目标光学特征化
       图像 + 光度多模态融合
       sim-to-real 与 synthetic benchmark 可信度
       姿态估计可观测性
       现代姿态反演/融合/序列/不确定性架构

B-2  方法总结
     基于 B-1 文献结论，不预设模型架构。

B-3  单帧多维 OCS 与光变曲线对比设计
     不是二选一；保留“单帧多维 OCS 继续做 + 光变曲线也做 + 二者对比”的选项。

B-4  模型改正候选
     分类/回归、序列模型、mid/late/cross-attention fusion、不确定性输出等，均待文献和方法总结后再定。
```

头B 红线：

```text
文献检索和方法总结可以先做。
模型改正、新数据生成、新训练、split 或架构变化必须另走 C 类阶段门。
不得直接在原 R04 代码/数据/成果上原地修改。
需要修改时必须另建副本或新脚本，保持 R04 负结果链可复现。
```

## 4. 两头衔接顺序

当前采用以下实际节奏：

```text
现在：
  头A 推 A-1：Codex 审 E45D-FIX01。
  头B 可并行启动 B-1：文献检索。

A-1 通过后：
  头A 推 A-2 / A-3。
  头B 继续文献检索，不启动新训练。

A-3 形成后：
  头B 接 B-2 / B-3 / B-4。
  因为 A-3 会定义负结果在三问中回答了什么、还缺什么。

两头都清楚后：
  做合并 Codex 审阅 / 总裁决。
```

## 5. 合并审阅触发条件

满足以下条件后，触发下一轮合并审阅：

```text
条件 1：头A 收口边界清晰
  E45D-FIX01 已裁；
  A-2/A-3 的完成状态或缺口清单明确。

条件 2：头B 后续方案清晰
  文献检索结论成形；
  方法总结草案成形；
  单帧多维 OCS vs 光变曲线对比方案和模型改正优先级有候选版本。
```

合并审阅将重新裁定下一阶段入口，包括但不限于：

```text
先完成 R04 线闭合；
启动方法改正；
设计单帧多维 OCS vs 光变曲线对比；
先做 V0.3 / V0.4 协议对齐核查；
或将以上任务排成新的阶段门。
```

当前不预设最终入口。

## 6. 当前立刻可执行事项

按本 R05，当前优先任务为：

```text
1. Codex 审阅 E45D-FIX01，形成路线一 R85 或对应返工单。
2. 并行启动文献检索任务，按 85 号文件给出的 6 个方向执行。
3. 暂不启动任何模型改正、新训练、正文正式改写或跨路线扩展。
```

## 7. CLAUDE.md 同步规则

本文件暂不要求立即修改 `CLAUDE.md`。

只有在以下情况之一发生后，才建议同步：

```text
E45D-FIX01 已正式审阅通过并形成稳定状态；
或头A/头B 合并审阅完成，产生新的稳定下一步。
```

同步时只写最新阶段状态、当前任务顺序和必要红线，不搬运 84/85/86/R04/R05 的全过程。

