# R139 Codex 审阅：005 不通过，旧 P4 需按最亮构型-光路机制口径返工

最后更新：2026-07-06  
审阅端：Codex  
审阅对象：

```text
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/02_Claude输出/005_P4_observation_planning_synthesis_Claude执行报告.md
v0.4_results/22_three_axis_p4_observation_planning/
```

后续更新：作者明确要求下一步不能只是材料整理，必须向最终物理目标推进。因此本文件第 4 节的窄返工提示词已由 `R140_Codex_任务单_P4PHYS最亮构型物理光路归因长程任务.md` 取代；随后考虑到 R140 一次性任务过长，实际执行进一步拆分，立即执行 `R141_Codex_任务单_P4PHYS-A_top1与roll局部确认.md`。本文件的“不接收 005/22、三轴小项目未收口、R128 继续挂起”裁决仍有效。

## 1. 审阅结论

005 报告与 22 号包不按当前三轴小项目最高口径接收，不能升级成果区，不能据此宣布三轴小项目阶段性收口，不能放行 R128。

本包可保留为旧口径 observation planning / role stratification 辅助材料，但不是当前 P4 的有效交付。当前有效 P4 必须按 R137/R138 与 2026-07-06 冻结指导文件执行：

```text
先确认 single-pose top-1 最亮 yaw/pitch/roll + sun/view 构型；
再解释入射方向、受光部位、材料/表面响应和探测器接收路径；
随后检验同类入射-表面/材料-探测器光路机制是否普遍对应高亮候选簇。
```

## 2. 不接收原因

### 2.1 必读链路不完整

005 报告的必读文件清单未列入 R138，也未体现 2026-07-06 冻结指导文件中“top-1 -> 光路解释 -> 机制普遍性检验”的新增口径。报告仍按旧 `P4 observation planning synthesis` 执行。

### 2.2 缺少当前 P4 必需产物

22 号包没有以下当前强制输出：

```text
tables/p4_global_brightest_pose_table.csv
tables/p4_brightest_roll_profile.csv
tables/p4_roll_aggregated_brightest_region.csv
tables/p4_brightest_light_path_trace.csv
tables/p4_brightest_surface_material_trace.csv
tables/p4_bright_mechanism_generality_check.csv
figures/p4_global_brightest_pose_panel.png/.pdf
figures/p4_bright_mechanism_consistency_map.png/.pdf
text/p4_brightest_configuration_summary.md
text/p4_bright_mechanism_generality_summary.md
```

22 号包的 gate matrix 仍按旧角色分层标准自检，因此 16/16 PASS 不适用于当前 P4 接收。

### 2.3 未确认 single-pose top-1

005 报告继续把 `yaw147.5/+12.5` 作为“最亮姿态 caution”讨论，但该点来自 roll-aggregated yaw/pitch 高亮区域，不等于当前主目标所需的 single-pose top-1。

按 P3 明细表 `p3_local_refinement_metrics.csv` 重排，当前已知 single-pose 前列为：

```text
top-1: R1_high_info, yaw=245.0, pitch=+30.0, roll=+15, ocs_total=2.083770e-01
top-2: R1_high_info, yaw=245.0, pitch=+32.5, roll=+15, ocs_total=2.079097e-01
top-3: R1_high_info, yaw=245.0, pitch=+35.0, roll=+15, ocs_total=2.062671e-01
```

R4 `yaw147.5/+12.5` 是 roll-aggregated high-brightness region，不能替代 top-1 三轴构型。

### 2.4 未做光路、材料/表面、探测器路径解释

22 号包只提供角色分层、优先级矩阵、risk boundary 和 observation planning summary；没有明确输出：

```text
太阳入射方向；
受光部位；
材料/表面响应；
探测器接收方向；
上述字段是否能从现有 P1/P2/P3 数据中直接追溯；
若不能追溯，缺失的最小字段清单。
```

不能用“亮度/信息/风险”角色分层替代物理光路解释。

### 2.5 未检验高亮机制普遍性

当前新增标准要求：top-1 确认后，检查同类入射-表面/材料-探测器光路机制是否对应一片普遍高亮候选簇。22 号包没有机制分类、候选簇亮度分布、邻域高亮比例、异常点解释或无法检验字段缺口。

## 3. 可保留内容

以下内容可作为 P4-FIX01 的辅助输入，不作为成果区稳定结论：

```text
p4_planning_candidate_roles.csv
p4_region_role_summary.csv
p4_observation_priority_matrix.csv
p4_risk_and_boundary_matrix.csv
p4_brightness_information_decoupling_summary.png/.pdf
p4_r128_interface_candidates.csv
```

它们只能服务于辅助标注：高信息、低信息、观测规划、R128 接口候选和 brightness != information 边界。

## 4. 返工任务：P4-FIX01 最亮构型与光路机制重聚合

交给 Claude 的任务如下。

### 4.1 任务代码

```text
three_axis_p4_fix01_brightest_configuration_light_path_mechanism
```

### 4.2 输出位置

执行报告写入：

```text
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/02_Claude输出/006_P4_FIX01_brightest_configuration_light_path_Claude执行报告.md
```

新结果写入：

```text
v0.4_results/23_three_axis_p4_fix01_brightest_light_path/
```

旧 22 号包只读，不覆盖。

### 4.3 必读文件

按顺序读取并在报告中列出：

```text
CLAUDE.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/00_路线冻结文件区/01_三轴小项目_最亮构型与观测规划指导文件.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/04_Codex审阅/R137_Codex_任务单_P4_observation_planning_synthesis.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/04_Codex审阅/R138_Codex_补充校正_三轴小项目最亮构型主目标与roll遍历口径.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/04_Codex审阅/R139_Codex_审阅_005不通过_旧P4需按最亮构型光路机制返工.md
v0.4_results/19_three_axis_p1_seed_roll_scan/tables/p1_seed_roll_ocs_table.csv
v0.4_results/20_three_axis_p2_sparse_grid/tables/p2_sparse_grid_metrics.csv
v0.4_results/21_three_axis_p3_local_refinement/tables/p3_local_refinement_metrics.csv
v0.4_results/21_three_axis_p3_local_refinement/tables/p3_high_brightness_refined_candidates.csv
v0.4_results/22_three_axis_p4_observation_planning/tables/p4_planning_candidate_roles.csv
```

若某个表不存在，必须列缺失路径和替代表，不得静默跳过。

### 4.4 允许与禁止

允许：

```text
1. 只读并重聚合 P1/P2/P3/旧 P4 表格。
2. 新增汇总、审计、制图和说明脚本到 23 号包。
3. 输出 top-1、top-N、roll profile、roll-aggregated region、机制普遍性检查和字段缺口表。
```

禁止：

```text
1. 不新增 Blender 渲染或后处理。
2. 不训练任何模型。
3. 不启动 R128、路线二/三/四或 T3/L2。
4. 不改 19/20/21/22 号包。
5. 不写成果区，不生成 Codex 审阅文件，不改 CLAUDE.md。
6. 不把缺失的光路/材料/表面/探测器字段编造成已知。
```

### 4.5 必做子任务

#### A. 输入与字段缺口审计

输出：

```text
audit/p4_fix01_input_manifest.csv
audit/p4_fix01_required_field_audit.csv
tables/p4_fix01_evidence_index.csv
```

必须标记哪些字段可直接得到，哪些字段缺失：

```text
yaw / pitch / roll；
ocs_total；
sun/view geometry；
glint / saturation；
camera/view trace；
illuminated part；
material / surface；
detector receiving path。
```

#### B. single-pose top-1 与 top-N 重聚合

输出：

```text
tables/p4_global_brightest_pose_table.csv
tables/p4_global_brightest_topN.csv
tables/p4_brightest_roll_profile.csv
tables/p4_roll_aggregated_brightest_region.csv
figures/p4_global_brightest_pose_panel.png/.pdf
text/p4_brightest_configuration_summary.md
```

必须回答：

```text
1. P1/P2/P3 范围内 single-pose top-1 是什么。
2. top-1 是否为 yaw=245, pitch=+30, roll=+15；若不是，给出证据。
3. roll-aggregated 最亮 yaw/pitch 区域是什么。
4. top-1 与 roll-aggregated 高亮区域是否一致。
5. top-1 是否伴随 glint/saturation 风险。
```

#### C. 光路、材料/表面、探测器路径解释

输出：

```text
tables/p4_brightest_light_path_trace.csv
tables/p4_brightest_surface_material_trace.csv
tables/p4_detector_receiving_path_trace.csv
text/p4_light_path_interpretation_or_gap.md
```

若现有数据不能追溯 part/material/surface/detector path，必须明确写成“当前数据字段不足”，并给出最小补充字段，不得把区域名或亮度排名当成材料/表面解释。

#### D. 高亮机制普遍性检验

输出：

```text
tables/p4_bright_mechanism_generality_check.csv
tables/p4_bright_mechanism_candidate_cluster.csv
figures/p4_bright_mechanism_consistency_map.png/.pdf
text/p4_bright_mechanism_generality_summary.md
```

必须先定义“同类机制”的可操作口径：

```text
优先：相同或相近入射方向 + 相同受光部位/材料/表面 + 相近探测器方向；
若字段不足：退化为同区域/邻近 yaw-pitch-roll/相近 glint-saturation 状态的 proxy 口径，并明确标为 proxy。
```

必须回答：

```text
1. 与 top-1 同类机制的候选簇是否普遍高亮。
2. top-N 中有多少来自同一候选簇或同类机制。
3. 若同类机制不普遍高亮，top-1 是否可能是局部 glint、饱和、遮挡边界或数值偶然峰。
4. 该机制结论能否写成物理解释，还是只能写成待补字段的假设。
```

#### E. 旧 P4 角色分层的辅助回接

输出：

```text
tables/p4_fix01_auxiliary_role_reconciliation.csv
text/p4_fix01_observation_planning_as_auxiliary.md
```

必须把旧 22 号包中的 C09、C01-C08、R3、R2/R5 改写为辅助标签，不能替代 top-1 和光路机制结论。

#### F. 验收与红线

输出：

```text
tables/p4_fix01_gate_matrix.csv
audit/generated_files_manifest.csv
audit/numeric_path_consistency_check.csv
audit/redline_self_check.csv
text/codex_review_checklist_for_006.md
```

## 5. P4-FIX01 接收标准

最低接收标准：

```text
1. 23 号包存在，006 报告写入正确路径。
2. single-pose top-1 / top-N 与 roll-aggregated 高亮区域分开输出。
3. top-1 光路、材料/表面、探测器路径给出解释或明确字段缺口。
4. 高亮机制普遍性检验完成，或明确说明只能做 proxy 检验。
5. 旧 P4 观测规划角色被降为辅助标注。
6. 未新增渲染、未训练、未启动 R128、未写成果区、未改 CLAUDE.md。
```

强接收标准：

```text
1. 能给出 top-1 是否属于一片高亮机制簇的清楚判断。
2. 能区分“物理光路解释成立”和“字段不足只能作为假设”的边界。
3. 能直接支持 Codex 下一轮裁决三轴小项目是否阶段性收口。
```

## 6. 当前阶段门状态

```text
005 / 22 号包：不接收为当前 P4。
三轴小项目：未收口。
R128：继续挂起。
成果区：不升级。
下一步：执行 R141 / P4-PHYS-A / 006A。
```
