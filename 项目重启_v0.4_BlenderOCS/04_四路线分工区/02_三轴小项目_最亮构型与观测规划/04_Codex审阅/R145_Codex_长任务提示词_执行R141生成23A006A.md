# R145 Codex 长任务提示词：执行 R141，生成 23A / 006A

最后更新：2026-07-06  
任务类型：给 Claude 的可直接执行长任务提示词  
上游依据：R139、R140、R141、R142、R143、R144  
本轮目标：执行 P4-PHYS-A，建立 23A 包并输出 006A 报告  

> 给 Claude 使用说明：请完整阅读并执行本文。本文是执行任务，不是讨论稿。若执行中发现阻塞，只能在 006A 报告中列明证据路径、阻塞点和最小补救需求；不得自行放行下一阶段，不得生成 Codex 审阅文件。

## 1. 本轮唯一目标

本轮不是材料整理，不是观测规划总结，不是完整光路归因。

本轮只做一件事：

```text
在固定 phase63/L1-G1 sun/view 几何下，
确认 yaw/pitch/roll 三轴姿态空间中的 single-pose top-1 最亮点，
并判断/执行必要的 roll/yaw/pitch 局部加密。
```

当前已知 P1/P2/P3 的 sun/view 几何固定为：

```text
SUN = [1, 0, 0.3]
DET = [0.5, -1, 0.1]
```

所以 006A 报告中的结论只能写成：

```text
固定 phase63/L1-G1 sun/view 下的 top-1 姿态候选/确认结果。
```

不得写成所有 sun/view 几何下的全局最亮构型。

## 2. 必读文件

按顺序读取：

```text
CLAUDE.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/00_路线冻结文件区/01_三轴小项目_最亮构型与观测规划指导文件.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/01_成果区/00_当前主用成果/05_三轴小项目最亮构型与光路解释技术路线_R144依据.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/04_Codex审阅/R139_Codex_审阅_005不通过_旧P4需按最亮构型光路机制返工.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/04_Codex审阅/R140_Codex_任务单_P4PHYS最亮构型物理光路归因长程任务.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/04_Codex审阅/R141_Codex_任务单_P4PHYS-A_top1与roll局部确认.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/04_Codex审阅/R142_Codex_审阅_R141讨论稿部分采纳但不替代006A执行.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/04_Codex审阅/R143_Codex_规划_R141_R142后固定几何最亮姿态确认执行方案.md
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/04_Codex审阅/R144_Codex_技术路线_三轴小项目最亮构型与光路解释.md
```

必须读取数据表：

```text
v0.4_results/19_three_axis_p1_seed_roll_scan/tables/p1_seed_roll_ocs_table.csv
v0.4_results/20_three_axis_p2_sparse_grid/tables/p2_sparse_grid_metrics.csv
v0.4_results/21_three_axis_p3_local_refinement/tables/p3_local_refinement_metrics.csv
v0.4_results/21_three_axis_p3_local_refinement/tables/p3_high_brightness_refined_candidates.csv
v0.4_results/21_three_axis_p3_local_refinement/tables/p3_region_summary.csv
v0.4_results/21_three_axis_p3_local_refinement/metrics/p3_metric_definitions_used.md
```

若执行局部加密，必须审计并复用/派生以下代码，不得直接改原始代码：

```text
v0.4_results/21_three_axis_p3_local_refinement/scripts/p3_render_local_refinement.py
v0.4_results/21_three_axis_p3_local_refinement/scripts/b_c_metrics.py
06_v0.4_code/00_config/config_v0_4.py
06_v0.4_code/02_blender/render_full_2664_shadow.py
06_v0.4_code/05_postprocess/run_full_postprocess.py
06_v0.4_code/05_postprocess/ocs_integration_v0_4.py
```

## 3. 输出位置

执行报告写入：

```text
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/02_Claude输出/006A_P4PHYS_top1_roll_confirmation_Claude执行报告.md
```

全部新结果写入：

```text
v0.4_results/23A_three_axis_p4phys_top1_roll_confirmation/
```

建议目录结构：

```text
v0.4_results/23A_three_axis_p4phys_top1_roll_confirmation/
  audit/
  tables/
  figures/
  text/
  scripts/
  logs/
  render/
  postprocess/
```

不得覆盖或改写 19/20/21/22 号包。

## 4. 允许与禁止

允许：

```text
1. 新建 23A 包中的脚本、表格、图、审计文件和报告材料。
2. 读取并重聚合 P1/P2/P3 明细表。
3. 派生 21 号包脚本到 23A/scripts/，只在 23A 内修改。
4. 若触发局部加密，执行受控小规模新渲染/后处理。
5. 复用已有 P3 数据点，避免重复渲染。
```

禁止：

```text
1. 不训练任何模型。
2. 不启动 R128。
3. 不启动路线二/三/四。
4. 不做全局 yaw/pitch/roll/sun/view 暴力遍历。
5. 不做完整物理光路归因；本轮只做归因可行性预检。
6. 不改 19/20/21/22 号包。
7. 不改 CLAUDE.md、成果区、Codex 审阅文件或路线总览。
8. 不生成任何 Codex 审阅/验收/最终放行文件。
9. 不把 fixed sun/view 结果写成所有 sun/view 全局最亮。
10. 不把 R4 鲁棒亮区写成并列 top-1，除非新加密数据实际超过 R1。
```

## 5. 必做任务

### A. 输入审计与来源覆盖

输出：

```text
audit/input_manifest.csv
audit/source_table_manifest.csv
audit/read_files_manifest.csv
audit/redline_precheck.csv
```

必须回答：

```text
1. P1/P2/P3 表是否存在且可读。
2. 当前 sun/view 是否固定为 phase63/L1-G1。
3. P1/P2/P3 的 ocs_total 是否同口径可比。
4. 当前任务是否仍限制在 fixed sun/view 下。
```

### B. 现有采样 top-1 / top-N 重聚合

输出：

```text
tables/p4physA_existing_global_top1.csv
tables/p4physA_existing_global_topN.csv
tables/p4physA_source_pack_coverage.csv
figures/p4physA_existing_topN_map.png
figures/p4physA_existing_topN_map.pdf
text/p4physA_existing_top1_summary.md
```

必须回答：

```text
1. 当前 P1/P2/P3 已有采样内 single-pose top-1 是什么。
2. 是否为 R1_high_info, yaw=245.0, pitch=+30.0, roll=+15。
3. top-1 与 top-2/top-3 的相对差。
4. R4 最高 single-pose 与 top-1 的相对差。
5. R4 是 roll-robust 高亮区还是 single-pose top-1。
```

当前 Codex 核验值应作为检查锚点，不可不核直接照抄：

```text
P3 sampled-grid top-1:
  R1_high_info, yaw=245.0, pitch=+30.0, roll=+15, ocs_total=0.208377

P3 top-2:
  R1_high_info, yaw=245.0, pitch=+32.5, roll=+15, ocs_total=0.2079097

P3 R4 最高 single-pose:
  R4_bright_info_boundary, yaw=147.5, pitch=+12.5, roll=-15, ocs_total=0.2018225
```

### C. Roll profile 与局部稳定性

输出：

```text
tables/p4physA_top1_roll_profile.csv
tables/p4physA_topN_cluster_roll_profiles.csv
tables/p4physA_R4_robust_bright_roll_profile.csv
tables/p4physA_local_peak_stability.csv
figures/p4physA_top1_roll_curve.png
figures/p4physA_top1_roll_curve.pdf
figures/p4physA_R1_R4_roll_curve_compare.png
figures/p4physA_R1_R4_roll_curve_compare.pdf
text/p4physA_roll_stability_summary.md
```

必须回答：

```text
1. R1 top-1 的 roll=+15 是否高于 roll=0 和 roll=+30。
2. R1 top-1 是否为尖锐 roll 峰。
3. 现有 roll 档位是否足以锁定最亮姿态。
4. R4 的 roll 稳健性如何。
5. R1 尖锐最亮峰与 R4 鲁棒高亮区是否应作为后续机制对照。
```

注意：

```text
R1 当前不能直接称为 glint 尖峰；若表中 glint_flag=0、saturation_flag=1，
应写作 roll-sharp / saturation-associated high-brightness candidate。
```

### D. 是否需要局部加密的数值决策

输出：

```text
tables/p4physA_refinement_need_decision.csv
tables/p4physA_refinement_candidate_matrix.csv
text/p4physA_refinement_decision.md
```

必须采用以下触发门：

```text
若 top-1 与 top-2/top-3 相对差 < 5%，触发加密；
或 top-1 与 R4 最高 single-pose 相对差 < 5%，触发加密；
或 top-1 roll profile 在相邻 roll 档出现尖峰，触发加密；
或未采样 roll（+10/+12.5/+17.5/+20 等）可能超过当前 +15，触发加密。
```

按当前已知数据，预期应触发局部加密。若你判定不加密，必须给出数值证据；不能只写“已有数据足够”。

### E. 局部加密执行

若 D 触发加密，先 smoke，再正式执行。

优先 R1 top 簇：

```text
yaw   = {242.5, 245.0, 247.5}
pitch = {27.5, 30.0, 32.5, 35.0}
roll  = {+5, +10, +12.5, +15, +17.5, +20, +25}
```

规模：

```text
3 × 4 × 7 = 84 单位
```

R4 少量机制对照：

```text
中心：yaw=147.5, pitch=+12.5
roll = {-30, -15, 0, +15, +30}
```

若资源仍充足，可扩为：

```text
yaw ∈ {145.0, 147.5, 150.0}
pitch ∈ {10.0, 12.5, 15.0}
roll 仅取少量代表档，不得挤占 R1 top 簇。
```

总规模上限：

```text
新增诊断渲染/后处理单位 <= 150。
若超过，优先保留 R1 top 簇 roll 细化，R4 只保留中心对照。
```

输出：

```text
scripts/p4physA_prepare_refinement_matrix.py
scripts/p4physA_render_refinement.py
scripts/p4physA_postprocess_refinement.py
scripts/p4physA_collect_refined_metrics.py
logs/p4physA_smoke.log
logs/p4physA_render_batches.log
logs/p4physA_postprocess_batches.log
tables/p4physA_refinement_render_manifest.csv
tables/p4physA_refined_top1_metrics.csv
tables/p4physA_refined_topN.csv
tables/p4physA_refined_roll_profile.csv
figures/p4physA_refined_top1_roll_curve.png
figures/p4physA_refined_top1_roll_curve.pdf
text/p4physA_refined_top1_summary.md
```

执行要求：

```text
1. 先做 1-3 个姿态 smoke，确认 label、渲染、后处理、ocs_total 读取链路可用。
2. smoke 失败时停止正式批量，写明阻塞和最小修复需求。
3. 正式执行必须记录每个 batch 的 exit code。
4. 已存在的 P3 点优先复用，不重复渲染。
```

### F. 加密后 top-1 稳定性裁决

输出：

```text
tables/p4physA_final_top1_decision.csv
tables/p4physA_boundary_followup_need.csv
text/p4physA_final_top1_decision.md
```

必须按以下规则：

```text
若新 top-1 位于 R1 加密网格内部，且相邻 roll/yaw/pitch 均未显示继续上升趋势：
  判定 fixed-geometry top-1 可进入 P4-PHYS-B 光路归因。

若新 top-1 从 roll=+15 迁移到 +10/+12.5/+17.5/+20，但仍位于内部：
  以新 top-1 作为 P4-PHYS-B 光路归因对象。

若新 top-1 落在边界，例如 roll=+5 或 +25，或 yaw/pitch 边界：
  不进入光路归因；建议下一轮只沿边界方向追加一小圈。

若 R4 对照超过 R1：
  重新定义 fixed-geometry top-1 为 R4 新点，并记录 R1 为尖锐高亮簇对照。
```

### G. 光路归因可行性预检

本轮不做完整归因，只预检下一轮能不能做。

输出：

```text
audit/p4physA_light_path_field_availability.csv
text/p4physA_next_physical_attribution_plan.md
```

必须回答：

```text
1. 当前已有 EXR/NPY/JSON 是否包含 per-part / per-material / normal / depth / object-id 信息。
2. 下一轮 P4-PHYS-B 是否需要新增 object/material/normal/depth pass。
3. 若需要，最小诊断姿态集是什么。
4. P4-PHYS-B 是否应同时归因 R1 top-1 与 R4 鲁棒亮区对照。
```

## 6. 验收与审计输出

必须输出：

```text
tables/p4physA_gate_matrix.csv
audit/generated_files_manifest.csv
audit/numeric_path_consistency_check.csv
audit/redline_self_check.csv
text/codex_review_checklist_for_006A.md
```

`p4physA_gate_matrix.csv` 至少包含：

```text
输入表存在且可读
P1/P2/P3 量纲/几何边界明确
current sampled-grid top-1 已输出
top-N 已输出
R1 roll profile 已输出
R4 roll profile 已输出
加密触发门已计算
若触发加密，smoke 已完成
若触发加密，正式矩阵已完成或阻塞已解释
refined top-1 已输出
边界 follow-up 是否需要已判断
光路归因字段可用性已预检
未训练
未启动 R128
未启动路线二/三/四
未写成果区/CLAUDE.md/Codex 审阅文件
```

## 7. 006A 报告格式

报告写入：

```text
04_四路线分工区/02_三轴小项目_最亮构型与观测规划/02_Claude输出/006A_P4PHYS_top1_roll_confirmation_Claude执行报告.md
```

报告必须简洁，建议结构：

```text
# 006A P4-PHYS-A top-1 与 roll 局部确认执行报告

## 1. 任务边界
说明 fixed phase63/L1-G1，不是全 sun/view 全局搜索。

## 2. 输入与脚本
列读取文件、脚本、数据表。

## 3. 现有采样 top-1 / top-N
给出 top-1、top-2、R4 最高点和相对差。

## 4. Roll profile 与加密决策
说明为什么需要或不需要局部加密。

## 5. 局部加密执行结果
说明 smoke、正式规模、exit code、refined top-1 是否迁移。

## 6. 最终 fixed-geometry top-1 裁决
明确是否可以进入 P4-PHYS-B 光路归因，或是否需要边界追加。

## 7. 光路归因可行性预检
说明下一轮需要哪些字段/pass/最小诊断姿态。

## 8. 红线自检
未训练、未启动 R128、未写成果区、未改 CLAUDE.md、未生成 Codex 文件。
```

## 8. 接收标准

最低接收：

```text
1. 23A 包存在。
2. 006A 报告存在。
3. current sampled-grid top-1/top-N 明确。
4. R1/R4 roll profile 明确。
5. 是否需要局部加密有数值门判断。
6. 若触发加密，已完成受控加密或给出真实阻塞。
7. fixed-geometry top-1 是否稳定有明确结论。
8. 下一轮光路归因字段可行性已预检。
9. 红线自检通过。
```

强接收：

```text
1. 加密后 top-1 位于非边界点，可直接作为 P4-PHYS-B 归因对象。
2. 明确说明 R1 top 峰与 R4 鲁棒亮区的角色。
3. 明确回答是否还需要继续遍历 roll。
4. 给出 P4-PHYS-B 的最小诊断姿态集与字段/pass 需求。
```

## 9. 最终提醒

```text
不要再写讨论稿。
不要只做材料整理。
不要进入完整光路归因。
不要启动 R128。
不要写成果区或 Codex 审阅文件。
本轮完成后等待 Codex 审阅 006A。
```

