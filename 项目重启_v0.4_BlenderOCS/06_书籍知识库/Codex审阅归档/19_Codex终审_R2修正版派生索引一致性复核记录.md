# 19 Codex终审：R2修正版派生索引一致性复核记录

最后更新：2026-06-23

## 1. 复核对象

复核目录：

```text
06_书籍知识库_R2-Codex修正版_2026-06-23/
```

复核依据：

```text
17_Codex审阅_R2修正版派生索引一致性问题清单.md
18_派生索引一致性修正完成报告_2026-06-23.md
```

本文只记录 Codex 终审结论，不修改正式 `06_书籍知识库/`，不修改原始图片，不裁定 P0 公式全文或表格数值。

## 2. 终审结论

```text
R2-Codex 修正版派生索引一致性：通过。
正式入库：仍需作者确认后选择性覆盖。
P0 公式全文和表格数值：仍保持待人工核对。
路线一 C：继续按 B0 provisional baseline 推进；B1/B2 后置。
```

## 3. 已通过的关键检查

### 3.1 表 3.1 替换污染

复核结果：

```text
bad_table_positive_files: 0
```

正式知识条目中未发现：

```text
表3.1（OCR修正）
OCR曾误作表3.1
表3.14种空间目标常用材质的BRDF峰值变化
材料参数系数表
```

说明：

```text
16/17/18 审阅或完成报告中仍会在“旧问题描述/验收说明”语境出现旧污染文本，不作为正式知识条目问题。
```

### 3.2 p63-p70 小节归属

复核结果：

```text
13_书籍知识库对v0.4主线的方法支撑与路线把控_R2候选.md 已同步：
p63 / 1709 -> 3.4 基于 Torrance-Sparrow 模型的改进 BRDF 经验模型
p64 / 1710 -> 3.4.1 Torrance-Sparrow 五参数模型
p65-p66 / 1711-1712 -> 3.4.1 五参数模型局限与改进推导
p67 / 1713 -> 3.4.1 改进六参数经验模型
p68-p70 / 1714-1716 -> 3.4.2 改进模型验证与材料样例
```

未发现 `1709-1716` 在 `02/05/11/13` 中正向继承为 `3.3.1` 的阻塞性残留。

### 3.3 B2 路线边界

复核结果：

```text
05/11/13 中 p63-p70 / 1709-1716 已统一转为：
B2 后续 BRDF 对照分支；
Discussion/Future Work；
不阻塞 Phase 0；
不进入当前 Phase 0 Method 主线。
```

未发现 `1710-1716 + 路线一C/前向模型` 的阻塞性残留。

### 3.4 证据等级

复核结果：

```text
evidence_A_05_11: 0
```

`05/11` 中 OCR 派生的公式、图表、模型条目已经从 A 收敛为 B。

### 3.5 生成脚本

复核结果：

```text
tools/apply_r2_codex_fixes.py 已增加 --force 保护。
表 3.1 二次替换污染规则已修正。
```

保留建议：

```text
tools/verify_r2_codex_consistency.py 在 Windows 默认 GBK 控制台下可能因中文输出触发 UnicodeEncodeError。
使用以下方式可正常运行：
PowerShell: $env:PYTHONIOENCODING='utf-8'; python tools/verify_r2_codex_consistency.py
```

该问题为工具输出编码问题，不影响本轮知识库内容验收。建议后续在脚本中显式设置 stdout UTF-8 或减少直接打印含特殊符号的全文行。

## 4. 入库前仍需作者确认

以下事项仍不能由 Codex 自动裁定：

```text
1. P0 核心公式全文、变量上下标、分式/指数结构；
2. 表 3.1 的 4 种材质名称、数值、单位和逐格准确性；
3. B1 改进冯模型式 (3.16)、式 (3.17) 的完整公式和参数解释；
4. B2 五参数/六参数模型式 (3.18)、式 (3.19)、式 (3.23) 的完整公式和参数解释；
5. 是否将 R2-Codex 修正版选择性覆盖正式 `06_书籍知识库/`。
```

## 5. 建议下一步

```text
Step 1：由作者确认是否进入“选择性覆盖正式知识库”阶段。
Step 2：若进入，先列出拟覆盖文件清单，不直接覆盖。
Step 3：只覆盖 00、02、05、10、11、12、13、14、15 这类已审阅文件；保留 16/17/18/19 为审阅记录或另行归档。
Step 4：覆盖前再次确认正式 `06_书籍知识库/` 当前状态，避免误删用户已有改动。
```

## 6. 最终判定

```text
R2-Codex 修正版：通过派生索引一致性终审。
正式入库：暂缓，等待作者确认和受控覆盖清单。
```
