# Codex审阅：13主线方法支撑文件补强记录

日期：2026-06-23

对象：

`06_书籍知识库/13_书籍知识库对v0.4主线的方法支撑与路线把控.md`

## 1. 审阅结论

原文件主口径基本正确，但更像全书路线映射索引，缺少作者核对后的 P0 主证据链摘要，也缺少面向路线一 C 后续写作和实现的显式闸门。

本次不是纠错式重构，而是前置约束补强：让后续 GPT/Claude/Codex 读取文件时，优先看到哪些内容可进 Method、哪些只能作 Discussion/Future Work、哪些必须先查代码。

## 2. 已补强内容

1. 在“禁止写成”中增加：
   - `v0.4 已采用表 4.1 材料参数`
   - `式 (4.2) 按源图叉乘直接使用`

2. 新增“P0 作者核对后主证据链”：
   - P0-1 OCS 定义
   - P0-2 BRDF 定义
   - P0-3 BRDF 测量公式
   - P0-4 经典冯模型
   - P0-5 改进冯模型 / B1
   - P0-6/P0-7 Torrance-Sparrow 与改进六参数模型 / B2
   - P0-8 面元 OCS 求和与非分辨仿真
   - P0-9 表 3.1 材质 BRDF 峰值变化

3. 新增“路线一 C 写作与实现闸门”：
   - Method 可直接采用的内容
   - B1 作为优先目标但需先查代码的边界
   - B2 暂不进当前 Phase 0 Method 主线
   - B0 仅保留为工程 smoke test、回归对照、消融或兜底 baseline
   - 表 4.1 只能作为书中参数样例，不能自动等同当前代码材料库
   - 式 (4.2) 采用作者确认的点乘修正版

## 3. 当前口径

- B0 不作为论文科学主线。
- B1 可作为路线一 C Method 优先候选，但必须与代码和实验 manifest 对齐后才能写成已实现。
- B2 作为后续 BRDF 对照、Discussion、Future Work，不进入当前 Phase 0 Method 主线。
- 表 4.1 是书中“部分卫星表面材质对应的模型参数”，不是 v0.4 当前材料库事实。
- 式 (4.2) 源图疑似叉乘排版错误，项目知识库按作者确认采用点乘。

## 4. 验收关注

后续若继续自动检查，建议区分禁止写成语境和正向知识条目语境。以下词组可以在“禁止写成”中出现，但不得作为正向声明出现：

```text
v0.4 已采用表 4.1 材料参数
式 (4.2) 按源图叉乘直接使用
路线一 C 已采用书中三部件材料参数
```

结论：`13_书籍知识库对v0.4主线的方法支撑与路线把控.md` 已补强为更适合后续主线写作和实现审阅的入口文件。
