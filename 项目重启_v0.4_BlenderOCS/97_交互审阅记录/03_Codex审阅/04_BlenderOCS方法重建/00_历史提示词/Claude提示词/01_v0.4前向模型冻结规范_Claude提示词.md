# 01 v0.4 前向模型冻结规范 Claude 提示词

你现在位于：

```text
项目重启_v0.4_BlenderOCS/
```

## 任务目标

本次任务只生成 v0.4 前向模型冻结规范和数据/manifest 字段规范，不写代码、不重跑实验。

你需要把前两轮排查和 Codex 复审意见整合成可执行的“方法冻结文件”，明确：

1. Blender 负责什么。
2. Python 公式负责什么。
3. OCS 积分连续公式和像素离散公式。
4. camera visibility、NoL/NoV、sun-side visibility/self-shadow 的定义。
5. GGX/Cook-Torrance 主 BRDF 与术语边界。
6. clean image 线性响应、log1p、PNG、训练输入和退化实验的关系。
7. v0.4 manifest / source_data 字段规范。

## 读取范围

先读最小启动集：

```text
CLAUDE.md
00_只打开本文件夹时的启动说明.md
00_v0.4总控流程.md
```

然后读本阶段必读文件：

```text
04_BlenderOCS方法重建/00_公式与Blender分工说明.md
04_BlenderOCS方法重建/01_模块A_B与统一前向模型对比决策.md
04_BlenderOCS方法重建/02_Blender采样选择与现实目标说明.md
03_全项目排查/06_方法思路类坑位与处置汇总_Codex.md
03_全项目排查/07_相似方法坑专项排查报告_Claude.md
04_BlenderOCS方法重建/03_v0.4前向模型冻结问题清单_Claude.md
03_全项目排查/08_Codex复审意见_相似方法坑专项排查.md
98_外部材料备份/00_备份清单.md
```

按需检查旧记录和代码快照，重点查公式、图像响应、manifest、split、latest-run：

```text
98_外部材料备份/00_项目指导文件/进度档案_仿真与反演_full.md
98_外部材料备份/02_0603汇报材料/20260603_项目进展汇报_v2_extracted_text.txt
98_外部材料备份/03_关键代码快照/02_blender/brdf_postprocess.py
98_外部材料备份/03_关键代码快照/03_inversion/train_cnn.py
98_外部材料备份/03_关键代码快照/补充实验代码/
```

不要扫描整个备份目录。只围绕当前方法冻结需要查证。

## 必须执行的 Codex 修正

必须吸收 `03_全项目排查/08_Codex复审意见_相似方法坑专项排查.md` 中 CR2-001 到 CR2-008。

尤其注意：

1. 不要把 `camera-visible only` 写成“不论 sun 方向都计入”。正确写法是：
   - mask：camera-visible object pixels。
   - contribution：如果 `NoL<=0` 或 `NoV<=0`，贡献显式为 0。
   - sun-ray self-shadow 是额外 macro-occlusion 选项。

2. 不要直接推荐放弃 sun-side visibility。
   - 必须列出 Blender sun-view/shadow pass、Python ray-cast、viewer-side only 三种方案。
   - 给出成本、验证方式、论文写作后果。
   - 不能只因复杂度直接选 viewer-side only。

3. 薄板可见性不能直接归因于“双面同时满足法线判据”。
   - 应写成旧记录提示的 thin-plate / double-sided / edge visibility 风险。
   - 关闭方式是 Blender depth + IndexOB sanity check。

4. Blender Cycles 背向面问题应写成 Python BRDF 后处理边界条件：
   - 确认 normal pass 坐标。
   - 显式过滤 `NoL<=0`、`NoV<=0`。
   - 不依赖 Blender Combined/material shading。

5. log1p 链不能留 `<value>` 占位。
   - 如果旧代码能查到 scale/max_val，写出具体值。
   - 如果旧链不一致，明确 v0.4 重新定义。

6. manifest/source_data 只写 schema，不填伪默认值。
   - 不要把 `split_seed=42`、某个 split_method 或几何列表写成已冻结事实，除非有明确决策依据。

## 输出文件

请生成：

```text
04_BlenderOCS方法重建/04_v0.4前向模型冻结规范_Claude.md
04_BlenderOCS方法重建/05_v0.4数据与manifest字段规范_Claude.md
```

## 文件 1：前向模型冻结规范必须包含

```text
1. v0.4 前向模型总图
2. Blender / Python / 反演代码分工
3. 坐标系与姿态定义
4. Geometry pass 字段：normal, depth, part ID, camera mask, optional sun visibility
5. OCS 连续公式
6. OCS 像素离散公式
7. pixel_area_projected 定义、NoV 抵消条件、edge pixel 处理
8. NoL/NoV 边界条件
9. sun-side visibility 三方案与当前决策建议
10. GGX/Cook-Torrance BRDF 主模型
11. G_Smith microfacet shadowing-masking 与 macro geometric occlusion 术语区分
12. clean image 线性响应与 log1p/PNG/训练输入关系
13. 必须做的 sanity checks
14. 写作边界：synthetic forward model, no real telescope validation
15. 未冻结项和需要作者/Codex 决策项
```

## 文件 2：数据与 manifest 字段规范必须包含

```text
1. v0.4 所有旧结果全封存，所有主结果重跑
2. manifest 字段 schema
3. source_data.json 字段 schema
4. run_id / method_version / split_id / seed 规则
5. 禁止 latest-run 自动发现
6. OCS source 与 image source 一致性规则
7. 每个 summary / figure source data 必须记录的字段
8. 输出目录命名规范
9. 待 Codex 复审项
```

## 严格限制

- 不要写代码。
- 不要重跑实验。
- 不要修改外部旧目录。
- 不要把旧结果作为 v0.4 主结果。
- 不要把尚未决定的方案写成已解决。
- 不要用“复用旧结果”口径；旧结果统一封存。

## 最后回复

完成后只总结：

1. 生成了哪些文件。
2. 哪些方法项已经给出推荐方案。
3. 哪些项仍需作者/Codex 决策。
4. 哪些地方需要 Codex 复审。
