# GPT 提示词：作者确认与数值审计

你现在不是从零写论文，而是在审计一篇已有的最终整合版 v0.1。

## 1. 请先阅读

如果这是新会话，请先阅读或要求作者提供以下文件内容：

```text
论文写作\02_后整合双线修订\GPT交互\00_GPT后整合总览.md
论文写作\02_后整合双线修订\00_后整合双线总览.md
论文写作\02_后整合双线修订\01_作者确认与数值审计\00_本阶段任务说明.md
论文写作\01_初稿生成与整合\最终整合\最终整合版_v0.1_基于GPT吸收Claude.md
论文改进\20260529_论文写作完整规划.md
论文项目总览 copy.md
论文改进\20260529_补充实验进度.md
```

如果你不能读取这些文件，请先向作者索要关键内容。`论文项目总览 copy.md` 较长，可先索要与当前任务相关的实验结果、方法细节和数值段落。没有阅读主稿和全局背景前，不要直接改写正文。

硬性约束：

- 未读主稿、全局背景和总览前，不得直接改写论文正文，只能列检查框架、问题清单或要求补充材料。
- 禁止编造实验结果、引用文献、方法细节、模型配置、期刊要求或任何作者尚未确认的事实。
- 本轮 GPT 输出必须交给 Codex 审阅整合后，才能进入主稿版本线；GPT 不得直接覆盖主稿。

## 2. 你的任务

请对最终整合版 v0.1 做“作者确认与数值审计”。重点不是润色，而是找出所有需要作者确认、数值核对、引用核验和可能引发审稿风险的地方。

## 3. 必须输出

### A. 作者确认项总表

列出所有 `[需要作者确认：...]`，并补充你发现的隐性确认项。

字段：

```text
ID | 位置 | 确认问题 | 为什么重要 | 风险等级 | 作者需要提供什么 | 建议处理
```

### B. 数值审计总表

列出所有数值。

字段：

```text
ID | 位置 | 数值 | 对应结论 | 证据状态 | 风险等级 | 建议处理
```

### C. 引用核验总表

列出所有 `[CITATION: ...]` 和 `[to verify]`。

字段：

```text
ID | 位置 | 引用占位 | 支撑的句子/表格 | 风险等级 | 需要查证什么 | 建议关键词
```

### D. 过度表述风险

检查是否存在：

- clean image 被写成真实场景性能
- OCS 被写成 universally robust
- fusion 被写成 universal superiority
- `all_raw` 被写成 operational feature
- `r = 0.003` 被写成 ResNet pair 证据

### E. 优先处理顺序

给出 10 项以内的优先处理清单，按投稿风险排序。

## 4. 禁止事项

0. 禁止编造实验结果、引用文献、方法细节、模型配置、期刊要求或任何作者尚未确认的事实。
1. 不新增实验数据。
2. 不发明参考文献。
3. 不直接把占位符替换为你猜测的事实。
4. 不重写全文。
5. 不删除限制条件。
